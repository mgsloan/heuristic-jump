# Campaign b5c7bae2-e62f-478a-8faf-182713552bd6

- loop: core
- phase: 1a
- opened: 2026-08-05T01:43:10+00:00
- commit at open: 46c1c44cbf2d09f952d04bf90bfbb40ea8c5ba11
- session: `claude --resume b5c7bae2-e62f-478a-8faf-182713552bd6`

## Target

Both assigned gaps:

- `core.md#the-dependency-graph[a7eaf1dfa1]` — `shared`'s authoritative list
  includes `rayon`, for a `ProjectView::scan` that "executes on the pool it is
  handed at construction".
- `core.md#the-trait[70bb31391f]` — `ProjectView` has no read cache, no parse
  LRU and no pool, all correct per `conformance-005`/`012`, so §1's bullet is
  the tense fix §9 already took.

Extended within the same two files (`crates/shared/{src,tests}/project.rs`) to
the rest of §1's `ProjectView` claims, and then — after
`harness/hj claim core ... 'core.md#4-project-file-enumeration'` was granted —
to the file list's multi-root behaviour, which is the same two files again.

## Hypothesis

`a7eaf1dfa1` is stale: the audit run that opened it read commit `fe25ac72`, my
own campaign `26e3bb3c` closed it with `82d2fe4` (CHANGE-core-029) minutes
later, and the later audit run did not re-read `#the-dependency-graph`. One
turn on `state/audit/gap-log.jsonl` settles it, and §9's spliced text already
names `rayon` as deferred.

`70bb31391f` is fresh — the audit saw my §1 commits `c616046`/`e124dc1` and
opened it anyway — and its `found:` is right. §1 said the driver "can […] cache
reads within a query, reuse the parse LRU […] and run the literal scan on the
bounded pool"; §8.4 had already taken that correction for the read cache and
left §1 standing. So: the tense fix, plus the mechanical check the gap names —
"a check that `ProjectView` holds no cache or pool field".

The wider hypothesis, carried from `26e3bb3c`: a claim stated as an equality
and checked as a subset is where the holes are. Applied here to a *field list*
rather than to a dependency list.

## Experiments

1. §1's bullet rewritten (CHANGE-core-035) + `the_view_holds_no_cache_and_no_pool`,
   a `name: Type` equality over `ProjectView`'s fields. Planted a fifth field:
   named. Green, committed `cd22272`.
2. `the_view_is_read_from_several_threads_at_once`. Planted a `RefCell` field:
   the **library built** and every test binary but this one compiled — nothing
   in the workspace had ever asked whether a `ProjectView` is `Sync`. Green,
   committed `2e7c656`.
3. `the_views_public_surface_is_what_a_query_gives_a_handler`, an equality over
   `impl ProjectView`'s public signatures. `FileList::paths` is `pub` and hands
   back every path in the project; the two-routes claim rests only on nothing
   returning a `&FileList`. Planted `pub fn files`: named. Green, `0e47b51`.
4. `a_parse_past_its_deadline_returns_a_tree_rather_than_an_ambiguous_none`.
   Planted the deadline check: named. Green, `2ea9469`.
5. `the_whole_query_crosses_threads_and_not_only_the_view`. Planting a `RefCell`
   in `CommitPolicy` **also** failed `driver/src/workers.rs:238`, so the draft's
   stated reason was wrong and was rewritten before committing. Green, `b0f4666`.
6. Nested workspace roots enumerate the same file twice, under two
   `ProjectPath`s. Observed on a scratch fixture: three of five files doubled.
   `DECISION-core-027` (open, class-b), provisional innermost-root-wins, one
   `continue` in the walk, `a_file_inside_two_roots_is_one_candidate`. Green,
   `68f599f`.
7. `an_open_document_is_parsed_across_its_chunk_boundaries` and
   `a_relative_path_that_could_escape_its_root_is_refused`. `FileText::Open`
   was reached by no test at all. Planted `.last()` for `.next()` in the read
   callback: named. Green, `6fea2b9`.
8. `candidates_from_another_root_come_last_and_in_a_fixed_order`. Deleting both
   `then_with` tie-breaks leaves the existing tier test passing. Green,
   `a04a7a9`.
9. `bytes_scanned` asserted `> 0` and claimed as an equality; made it the sum
   of the candidates' sizes. Planted a hits-only count: 102 against 145. Green,
   `f0e892f`.

No experiment was reverted. Two passed `cargo test` and failed the gate on lint
(`redundant_clone`, `cast_possible_truncation`); both were fixed in place.

## Outcome

**confirmed.** Nine commits, all green, 342 → 350 tests, one Class A change
(CHANGE-core-035), one Class B escalation (`core-027`).

`70bb31391f` is closed by the tense fix and the field equality. `a7eaf1dfa1`
was already closed before this campaign opened and needed no work — the
gap-log check cost one turn and freed the session.

The assignment's expected reading held: everything is
`crates/shared/{src,tests}/project.rs` and `design/core.md` §1/§9, except that
the multi-root work is attributed to `core.md#4-project-file-enumeration`,
claimed and granted rather than taken.

What the campaign is really evidence for: **a section whose assigned gap was
stale still had eight unheld claims in one file.** Seven of the nine commits
are claims the audit never named, and two of them (`Sync`, `FileText::Open`)
were untested *branches* rather than weak tests.
