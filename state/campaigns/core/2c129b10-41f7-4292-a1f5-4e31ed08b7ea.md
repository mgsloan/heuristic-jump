# Campaign 2c129b10-41f7-4292-a1f5-4e31ed08b7ea

- loop: core
- phase: 1a
- opened: 2026-08-04T04:18:23+00:00
- commit at open: f8e843bd894b0d2d07767724745b4eb47bec35aa
- session: `claude --resume 2c129b10-41f7-4292-a1f5-4e31ed08b7ea`

## Target

The planner's assignment, both gaps:

- `core.md#85-the-untagged-unions-are-the-actual-risk[cfd3a3fbdc]` — the golden
  corpus holds six captured lines, all rust-analyzer, where §8.5 names three
  servers and two editors.
- `core.md#84-location-is-byte-based-and-this-fixes-a-real-inconsistency[f2b9c0b7e5]`
  — the carried row and the conversion's second read.

Shared context, which is the test for taking both: `crates/shared/src/proto.rs`
and the two things built on it — `crates/shared/tests/differential.rs`, which
reads the corpus, and `crates/driver/src/dispatch.rs`, which is §8.4's
conversion. Both gaps are about what the wire types do with real input.

## Hypothesis

`[cfd3a3fbdc]` is closeable by capture rather than by argument: campaign
`acb37d9b` found rust-analyzer installed and recorded six lines, and if `gopls`
and `pyright` can be obtained the same procedure applies unchanged — the corpus
header documents it and nothing in `differential.rs` knows where a line came
from. What cannot be captured here is the *client* half: `initialize` params
and document traffic are composed by an editor, and the only editor on this
machine is a GUI one on the user's own display.

`[f2b9c0b7e5]` is suspected **stale**. Its `found:` says "`Location::line()` is
never read by the driver", and the audit that opened it ran at
2026-08-03T09:16, before campaign `acb37d9b` landed `fe50a83`. At head
`dispatch::encode` reads the carried row, compares it against
`Location::line_in` of the text it actually read, and fails with
`EncodingError::LineDisagreesWithRange`;
`driver/tests/wire_locations.rs::a_target_file_that_moved_under_the_query_is_refused_rather_than_encoded`
asserts it. The gap's *claim* — "nearly free because the text is already in the
view's cache" — is a sentence §8.4 no longer contains: it was retracted under
`conformance-005` and the section now says the conversion re-reads.

## Experiments

1. **Captured pyright and gopls traffic** (`[cfd3a3fbdc]`). `gopls` v0.23.0
   via `go install`, `pyright` 1.1.411 via `npm install`; both driven over
   stdio by the corpus header's procedure, which is right except that pyright
   emits no `$/progress` and needs a fixed wait. Six captured lines from two
   servers that had none. The differential passed over them unchanged —
   nothing in `proto.rs` moved. Gate green, `eb4148b`.
2. **The three named servers, asserted by name** (`[cfd3a3fbdc]`). §8.5 names
   rust-analyzer, pyright and gopls specifically, and the corpus could have
   lost two of them with every test passing. The server is now parsed out of
   `source`. Gate green, `61fbc98`. Two mutations, both caught.
3. **The editor half, escalated** (`core-018`). `initialize` params and
   document traffic are client-composed; the only editor here is a GUI one on
   the user's live display. Decision record rather than a hand-authored line
   labelled CAPTURED. Gate green, `afab1f5`.
4. **§8.4's same-document short circuit** (`[f2b9c0b7e5]`). Class A
   (`CHANGE-core-005`): the section says the conversion "re-reads the target
   file" without exception, where `target_text` clones the snapshot's rope for
   the query's own document. Document edited, code not — and the test that
   makes the sentence checkable deletes the file from disk first. Gate green,
   `f4b9bd6`. Mutation: disabling the short circuit fails it and nothing else.
5. **Three more captures: what a server says when there is nothing to go to**
   (`[cfd3a3fbdc]`). Neither gopls nor pyright answers `[]`; both answer
   `null`. pyright answers `print` with two locations 2075 lines into
   typeshed, which broke the differential's 96-row `GRID` exactly as that
   fixture predicted. Gate green, `0881743`.
6. **The §8.5 minor: a CAPTURED label must agree with its message**. A
   relabelled hand-authored line satisfied the filter. A captured
   `InitializeResult` whose `serverInfo` names a server must name the one its
   label claims. Gate green, `65c4358`. Mutation caught.
7. **The §8.4 minor: `proto.rs` claimed a guarantee the types do not give**.
   `PositionEncoding`'s variants are public unit variants, so the type-level
   argument collapses; the mechanism is `seam.rs`'s scan. Comment only. Gate
   green, `06ef3a6`.
8. **`core.md#vocabulary-types`, claimed and granted** — and its gap turned
   out already closed (`shared.rs:54` re-exports all seven; `seam.rs:24` names
   them). Its *minor* was real and is the same defect as 7: `LanguageId` was
   documented as comparing by pointer where it compares by text, which is what
   the registry depends on across a crate boundary. Gate green, `dd3fa7b`.

## Outcome

**Confirmed.** Both assigned gaps are addressed, along with both of their
minor items and a third section's minor.

`[cfd3a3fbdc]` — the corpus now holds nine captured messages from all three
servers §8.5 names, with two tests that fail if a server goes missing or if a
label disagrees with the message under it. The editor half is `core-018` and
is not closeable by a loop; the journal says how a human does it in one
sitting.

`[f2b9c0b7e5]` — **was already closed** when this campaign started, by
`fe50a83` two audit runs earlier. What this campaign added is the exception
the section's universal claim got wrong, and the test for it.

The finding worth carrying: three of the gaps touched here were stale, and
`state/audit/core.toml`'s per-section `last_audited` predicts which. The gap
list handed to a worker is not re-derived against head.

