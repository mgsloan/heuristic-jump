# Campaign 7fda63d7-fc75-4469-9bc5-ac456b8a0143

- loop: core
- phase: 1a
- opened: 2026-08-05T01:43:10+00:00
- commit at open: 46c1c44cbf2d09f952d04bf90bfbb40ea8c5ba11
- session: `claude --resume 7fda63d7-fc75-4469-9bc5-ac456b8a0143`

## Target

`core.md#86-modelling-errors-must-fail-closed[5746a16de3]` — the `didSave`
checksum, the half of §8.6's third self-check that costs a read.

The other three assigned gaps —
`core.md#84-location-is-byte-based-and-this-fixes-a-real-inconsistency[9807aee0b3]`,
`core.md#snapshots-are-o1-to-take-and-are-parsed-before-a-handler-sees-one[02c4cde612]`,
`core.md#text-and-tree-can-never-disagree[a0661e4b63]` — are stale. All three
opened at `2026-08-04T23:43:10` against `4266031`; campaign `2f7fcfdd` built
the worker pool 79 seconds later (`03217e1`), and no audit run since has
re-judged those sections. They are held twice over:
`driver/tests/actor.rs::the_parse_and_the_conversion_never_run_on_the_thread_that_owns_the_state`
asserts the handler's thread on a real run against a target file in a *second*
file, and `driver/tests/seam.rs::only_the_pool_realises_a_seed_or_calls_the_dispatch_wrapper`
holds the source side. Nothing to do for them.

## Hypothesis

The `didSave` gap is the one thing in this assignment that is genuinely
unbuilt, and `actor.rs` names its own blocker: "a `Job` *is* a query — a
handler, a seed, a position and an answer — and a checksum read is a second
kind of job with a second kind of reply, which nothing builds". So the work is
a second job kind in `workers.rs`, not a read anywhere.

The part that is not in the gap text and that I expect to be the real content:
**the read is asynchronous now, so the comparison can race the buffer.** A
`didChange` between the `didSave` and the worker's read makes the file and the
rope differ legitimately, and comparing them then distrusts a document that
never drifted — §8.6's rule firing on its own latency. `TreeFate` already
argues that a version comparison does not close this (a `didOpen` resync
replaces the text at a version we have seen), so the guard has to be a stamp
of our own.

## Experiments

1. **`Generation` on the document row** (`9ff22ac`). The stamp that makes an
   asynchronous save check safe, plus the guard in `Documents::checked`. Gate
   green, +1 test. Plant-verified twice: the guard disabled fails the first
   assertion, and a resync that keeps its old stamp — which is what a version
   comparison amounts to — fails the second.
2. **The second kind of job** (`2e8c082`). `Job` and `Finished` become enums;
   `SaveJob` reads the file on a worker; `Actor::check_saved` dispatches it and
   `Actor::finished_save` compares. Gate green, +1 test. Plant-verified: the
   comparison dropped lets a query reach an untrusted document, and settling
   the checksum on `core`'s thread leaves the pool's channel empty.
   `clippy::large_enum_variant` fired on both enums and is `#[expect]`ed — the
   lint's remedy is a `Box` on the *common* variant.
3. **The source scan** (`9aabbb6`). `Documents::checked` is reached from
   `src/`, and `actor.rs` names no `fs::`. First attempt used `read_dir`,
   which `clippy.toml` denies; modules are enumerated through the library
   root's `mod` declarations instead. Gate green, +1 test.
4. **CHANGE-core-037** (`66fcc4d`). §8.6's lead-in claims all three self-checks
   are `core`-side O(1); the third bullet says it costs a read and belongs in a
   worker. Class A, recorded, with the declaration that this campaign wrote the
   code the section describes.
5. **The stale module header** (`7dd7bc4`). `documents.rs` still said the actor
   was missing.
6. **Two kinds on one channel** (`be2408f`). A checksum and a query outstanding
   together. Found and fixed an unsound assertion of my own: `try_recv` for
   "nothing was dispatched" races the worker, and a plant proved it by passing.

Not attempted, and why, in the journal: skipping the checksum in
`--proxy-only`; capping outstanding checks; incremental reparse.

## Outcome

`confirmed`. The one live gap in the assignment — §8.6's `didSave` checksum —
is built, tested three ways and held by a source scan. The other three were
stale before the campaign opened and are held twice over already; that is
established in the Target section above rather than by anything done here.

The most useful thing found that is *not* in the diff: **§2's argument for
eager parsing rests on a claim this driver makes false.** "The parse is usually
incremental from a cached base" — but `Actor::notified` forgets the cached tree
on every `didChange`, because `Documents::changed` applies the edit without
handing back the `InputEdit`s, so `core` has no edit log to seed with. The code
knows (`Actor::edits` documents it and calls it a latency question); the
document does not. It is not a spec fix — editing §2 toward the code is the one
shape the audit cannot catch — and it is not this campaign's to optimise:
`CLAUDE.md` wants a benchmark first. It is the next campaign's best target in
these files.
