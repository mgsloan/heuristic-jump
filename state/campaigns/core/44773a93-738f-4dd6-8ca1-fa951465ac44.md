# Campaign 44773a93-738f-4dd6-8ca1-fa951465ac44

- loop: core
- phase: 1a
- opened: 2026-08-04T04:59:56+00:00
- commit at open: c4dda397b45fb8b9347cd1a9b5eb6bfc22e2fc25
- session: `claude --resume 44773a93-738f-4dd6-8ca1-fa951465ac44`

## Target

- `core.md#85-the-untagged-unions-are-the-actual-risk[cfd3a3fbdc]` — the golden
  corpus holds six captured lines, all rust-analyzer server-to-client. §8.5
  names rust-analyzer, **pyright and gopls**, and asks for editor traffic too.
  The section's minor also stands: the enforcing test filters on the `source`
  string, so a relabelled hand-authored line satisfies it.
- `core.md#83-the-wire-position-type-is-inert[f9ad1766b7]` — the section is
  `clean` with a minor: nothing settles a `PositionEncoding`, so "set once from
  `InitializeResult` and never inferred" is unexercised rather than upheld.

## Hypothesis

Both of §8's remaining protocol-type items are about **evidence, not types**.
The unions are modelled correctly and tested; what is missing is (a) traffic
nobody here composed and (b) a site that settles the encoding the whole of §8.3
is written against.

## Why these together

Shared files: `crates/shared/src/proto.rs`,
`crates/shared/tests/differential.rs`, `crates/shared/tests/proto.rs`,
`crates/shared/tests/golden-traffic.jsonl`, `design/core.md` §8. The settling
function §8.3 wants is compared by the same differential that reads the corpus
§8.5 wants grown, and the newly captured `InitializeResult`s are exactly the
inputs settling has to handle — gopls says `utf-8`, pyright says nothing at
all, which is the LSP default §8.3 defers to the settling site.

## Experiments

1. Captured real server-to-client traffic from gopls v0.23.0 and pyright
   1.1.411 with a throwaway stdio client in `/tmp`, appended 12 lines to
   `golden-traffic.jsonl`. Gate green, committed.
2. Turned `the_corpus_holds_traffic_nobody_here_composed` from a label check
   into a content check: the captured `InitializeResult`s must name the three
   servers §8.5 lists, read out of `serverInfo` rather than out of `source`.
   Gate green, committed with experiment 1.
3. `PositionEncoding::settle(&ServerCapabilities)` — one site applying LSP's
   "absent means utf-16", with the differential asserting it against
   `lsp-types` over every captured and hand-authored `InitializeResult`.
4. Class A spec edit: §8.3 said "private fields and no accessors" where §6's
   predicate requires reading the child's row without the child's document.

## Outcome

See `state/journal/core-1.md` and the close below.
