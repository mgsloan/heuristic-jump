# Campaign 18835da5-abcf-4eed-bc64-f52405edd53f

- loop: core
- phase: 1a
- opened: 2026-08-04T04:18:23+00:00
- commit at open: 2fa2b0c9049ec8d7e3e77c8c29d5b2022dfd7f6f
- session: `claude --resume 18835da5-abcf-4eed-bc64-f52405edd53f`

## Target

The assignment, all of it — measure_core's replay half:

- `core.md#the-command-line[d2a209c7a8]` — replay is deterministic, byte for
  byte.
- `core.md#two-modes-collect-and-replay[f2e74dce26]` — `truth.jsonl` carries
  its provenance in a header record.
- `core.md#two-modes-collect-and-replay[eb424449b6]` — replay refuses a truth
  file whose repository commit does not match the checkout.
- `core.md#the-table-is-not-enough-a-replay-has-to-show-its-failures` —
  unjudged.

One piece of work: `table.rs`, `replay.rs`, `truth.rs`, `collect.rs` and
`tests/pipeline.rs`, which is the same reading four times over.

## Hypothesis

**The three gaps were closed by commits made after the audit read them, and
what is left in these files is different from what the gap text says.** The
audit stamps are `the-command-line` at 09:16Z and `two-modes` at 20:56Z;
`a96ffb2` took the wall clock out of the table at 19:09Z and `55de8a2` made
the provenance header the thing a resume is checked against at 21:18Z. So the
literal findings — a p50/p99 in the table, a `grammar` literal, a
`check_resumable` that compares only `server_version` — are gone.

What a re-audit should still find, and what this campaign is about:

1. **The header's own repository name is never checked.** `replay` verifies
   `commit`, `server` and `language` against the run; `repository` is the one
   field of the header nothing compares, so a truth file moved between two
   repository slots is refused only if the two happen to be at different
   commits.
2. **§7 says `heuristic_latency_us` is "the one field in the record that a
   replay does not reproduce exactly", and `stage_us` is a second one** —
   §7's own field table calls it "wall clock per pipeline stage,
   handler-supplied". The determinism test masks one field because the
   sentence names one field. Class A.
3. **Nothing holds the join the failure digest is built from.** The unjudged
   section says each group carries "a small seeded sample of concrete cases —
   repository, file, line, the identifier, what we returned, what the server
   said", and the digest is the harness's job. Three of those six are not
   fields of the record, and whether they are *recoverable* from what a replay
   writes is the claim `measure_core` owes the harness.

## Experiments

0. **The tree was red before anything was tried.** `harness/gate core` fails at
   HEAD: `driver::seam`'s §9 check names `heuristic_jump` as the only crate
   that may reach `tracing_subscriber`, and `087fa45` gave `measure_core` one.
   The seam test now reads a list of the crates that own a command line, which
   is what §9 is actually about. Gate green, committed `3a74ab5`.
1. `replay` checks the header's `repository` alongside `server` and `language`,
   and the identity checks run before the checkout is verified. Gate green,
   committed `c67c94c`.
2. Class A (CHANGE-core-009): §7 named one field a replay does not reproduce
   and there are two. The determinism mask is now a list, with a test holding
   what the list costs. Gate green, committed `7759197`.
3. The digest's concrete sample, recovered from a `--records` line plus the
   corpus, as the harness would. Gate green, committed `2070474`.
4. `collect` appended one row per two hundred, so a crash left a file that was
   not a prefix of the answers. `Writer` owns the rows now. Gate green,
   committed `4c50a45`.
5. A row whose file the checkout cannot read is refused rather than skipped:
   skipping takes it out of the denominator, which reads as coverage that
   improved. Gate green, committed `7431040`.
6. A corpus of two repositories, which is where replay order stops being free
   and where `--repo` becomes observable. Gate green, committed `c95b8b1`.
7. Class A (CHANGE-core-006): a truth row is not §7's record half-filled, and
   §8.2 is why. No code changed. Gate green, committed `6555b59`.
8. Claimed `core.md#one-measurement-library-…` for its minor: which language a
   run is about, answered once instead of twice. Gate green, committed
   `0ff448f`.

No experiment was reverted.

## Outcome

`confirmed`. The hypothesis held: three of the four assigned targets were
already substantially closed when the campaign opened, by commits made after
the audit read them, and what a re-audit would still have found was different
from what the gap text said. Every target now has the claim held by something
that fails when it stops being true.

The three findings that were not in the gap list, and are the reason the
campaign was not a formality:

- **The gate was red at HEAD**, from a campaign that closed `confirmed`. That
  is worth more attention than anything else here: the previous session either
  did not run the gate or ran it before its last commit.
- **`collect` lost up to 199 answers per crash** and left a truth file a
  resume misreads as a prefix. Each of those rows is a real server round trip,
  and the failure only shows up in the case checkpointing exists for.
- **`replay` silently shrank its own denominator** when a recorded file could
  not be read.

Left for a separate campaign, deliberately: everything `collect` does with a
live server is untestable here (`Collection::run` needs one), so `--restart`,
the probe loop and the resume arithmetic are held by construction and by
reading rather than by tests. And `harness/measure` — which §7's failure digest
needs — is `core-001`, open, and denied to every loop.
