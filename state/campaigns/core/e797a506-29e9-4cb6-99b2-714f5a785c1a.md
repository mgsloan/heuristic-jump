# Campaign e797a506-29e9-4cb6-99b2-714f5a785c1a

- loop: core
- phase: 1a
- worker: 2
- opened: 2026-08-04T00:35:11+00:00
- commit at open: 3cbd3358a41df1c467aa8ae717b80733e02cbb4b
- session: `claude --resume e797a506-29e9-4cb6-99b2-714f5a785c1a`

## Target

The planner's assignment, verbatim:

- `core.md#two-modes-collect-and-replay[f2e74dce26]` — grammar revision in the
  provenance header
- `core.md#two-modes-collect-and-replay[eb424449b6]` — a resume that moved the
  commit
- `core.md#the-command-line[d2a209c7a8]` — the printed table is deterministic
- `core.md#what-the-templates-handler-does[9adb0be268]` —
  `Stratum::Unimplemented` as a gate check
- `core.md#the-table-is-not-enough-a-replay-has-to-show-its-failures` —
  unjudged

Shared context as named: `crates/measure_core/src/{collect,truth,replay,table,
record,cli,corpus}.rs` and `crates/measure_core/tests/pipeline.rs`.

## Hypothesis

**All four gaps were already closed, and the audit that listed them is stale.**
The stamps say so: `#two-modes` and `#what-the-templates-handler-does` were
audited at 2026-08-03T20:56Z and `#the-command-line` at 09:16Z, while
`55de8a2`, `933a4aa`, `24e79cd` and `6c65912` — campaign `ff3e1a40`'s
follow-on commits, which closed `partial` — landed at 21:18Z through 21:34Z.
Reading the four sites confirms it: `grammar_pin` derives the pin from the
embedded lockfile, `Provenance::drift` destructures every field including
`commit`, `Table` holds counters and no `Duration`, and `Table::template`
returns a three-state verdict that a failing handler cannot counterfeit. Each
has a test, and `cargo nextest run -p measure_core` is 27/27 green at open.

So the useful work is not the gap list. It is the claims in these same five
sections that **still have no test**, found by reading the sections rather than
the audit:

1. **"Replay enforces no deadline at all"** (`#two-modes`) — the section calls
   this "the constraint that makes replay worth having, and it is easy to get
   wrong by doing the obvious thing". `Deadline::none()` is held by
   `crates/driver/tests/deadline.rs`; that a *replay* hands one to the handler
   is held by nothing.
2. **The digest's grouping key is mechanically available**
   (`#the-table-is-not-enough`) — abstentions by `(stratum_prior, reason,
   stages)`. The reason is carried inside `stages` rather than in a column of
   its own, and nothing asserts it arrives there at all.
3. **The mismatch half of the same key** — `(stratum_final, agreement,
   severity, stages)`, with `match_contained` kept apart from `mismatch`.
   Every fixture row today is a mutual "no definition here", so `severity` is
   `null` in every record any test has ever read, and `Agreement::classify` is
   never reached through a replay.
4. **`harness/measure`** — the section makes the digest the harness's job and
   names the script. It does not exist, and `harness/**` is denied.

## Experiments

Ten, all green, all committed. Every one was controlled — the planted failure
is named in each commit message.

1. `586188f` — replay hands the handler an unbounded deadline, and builds no
   bounded one anywhere. Two assertions: the record, and a scan of `replay.rs`
   for the parse deadline no record can see.
2. `d615c86` — the digest's coverage key: two abstain reasons, two clusters,
   the reason inside `stages`.
3. `c76c88c` — the digest's precision key: one corpus, five clusters, two
   differing only in `severity`. First test to reach `Agreement::classify`
   through a replay at all.
4. `9b7f622` — a corpus the oracle never answered: nine zero rows, the
   uncollected count in both renderings, `nothing measured` as a third state,
   exit zero.
5. `f43cdf1` — decision record `core-001`: `harness/measure` does not exist and
   `harness/**` is denied.
6. `86ba6a5` — the three ways a replay refuses a truth file: commit moved,
   collection unfinished, checkout dirty. The first is the sentence §7 actually
   states and only its *resume* half was held.
7. `1b5379b` — §7's usage block parsed out of `design/core.md` and compared
   against `clap`'s flag set, per subcommand; plus the defaults. Holds "there
   is no `--held-out` flag" as a closed set rather than a named absence.
8. `171a58c` — the seed decides the sample and a second seed decides another.
   Every other fixture passed a limit above what the repository holds, so the
   sampler was never reached.
9. `c96b666` — the corpus's notion of a query joined to the handler's, position
   by position. The test named for this claim previously only counted classes.
10. `b302025` — **a functional gap, not a test**: `measure` installed no log
    subscriber, so §7's replay wall clock and every warning this crate emits
    went into a facade with no dispatcher. Installed in `measure_core::run`,
    default `info`, `RUST_LOG` overrides, no new flag.

## Outcome

`confirmed`, with the hypothesis confirmed too: none of the four assigned gaps
was open. All four were closed by `ff3e1a40`'s follow-on commits, hours after
the audit that listed them, and each already had a test. Verified by reading
the four sites before writing anything.

The campaign is therefore ten claims from the same five sections that had no
test at all, plus one that had no *implementation* — the log subscriber, which
is the only place the assignment turned out to be about code rather than
coverage.

Left deliberately, and both are a fresh reading rather than an extension:

- **`collect` is undrivable from a test.** `Collection` is `pub(crate)` and the
  only way in is `resolve_server`, which reads the real `servers.toml` — not in
  this loop's write list, and a fixture server does not belong in it anyway.
  Everything `collect` owns (checkpointing, resume, `wait_until_useful`, the
  four outcomes) is held only through `check_resumable`.
- **The instantiated template is asserted by a double.** `lang_rust` says "no
  tests, deliberately", and `measure_core` may not depend on a language, so
  "an instantiated template runs under `measure_<x> replay`" would be a test in
  `crates/measure_rust/tests/` with the fixture corpus duplicated into it.
