# Campaign c601eeec-b30f-479c-8a7d-49e19e4c166d

- loop: core
- phase: 1a
- opened: 2026-08-04T04:59:56+00:00
- commit at open: 140039bf91ff610861243835ea347b33435cd161
- session: `claude --resume c601eeec-b30f-479c-8a7d-49e19e4c166d`

## Target

The planner's assignment, four gaps across three sections:

- `core.md#adding-a-language[68be1693b1]`
- `deps.md#14-workspace-cargotoml-shape[cda6a3e9e2]`
- `deps.md#14-workspace-cargotoml-shape[4e5b9e1cfa]`
- `deps.md#licensing-our-crates-are-mit-the-binary-is-gpl[3723863fd7]`

**Three of the four ids do not exist in this branch's
`state/audit/gap-log.jsonl`.** Only `3723863fd7` does. The last audit recorded
on this branch ran at `3dba8fae`, which is not an ancestor of it, so the ids
the planner quotes come from an audit of a sibling tree this checkout cannot
read. What this checkout's audit *does* record for these sections is already
closed here: `deps.md#14`'s only gap was `f288bd5296` (`serde_json` and the
proc-macro three missing from `[profile.dev.package]`), closed by campaign
`a9937015`; `core.md#adding-a-language`'s only gap was `0858868078`
(`heuristic_jump` not depending on `lang_rust`), closed by `b59733c6`.

So the target is taken as the *sections* rather than the ids: every claim §14,
the licensing subsection and `#adding-a-language` make is checked against the
tree, and whatever is unbacked is mechanized or fixed.

Shared context, which is what makes this one campaign: all three sections
mechanize as manifest scans in `crates/driver/tests/seam.rs`, over the same
files — the root `Cargo.toml`, the nine member manifests, `design/deps.md`
§5/§14 and `design/high-level.md`.

## Hypothesis

The unbacked claims are the ones no scan quantifies over:

* §14's manifest-shape bullets that no test reads: `resolver = "3"`,
  `[workspace.package]`'s contents, `[profile.release]`'s three values,
  `doctest = false`, the `cargo-machete` metadata table, and the
  `rust-toolchain.toml` pin its file tree names.
* The licensing subsection's one claim that is not about a manifest field:
  "the GPL commitment is stated plainly in `high-level.md`, and there are two
  GPL inputs, not one". `high-level.md` still carries the superseded position
  `deps.md` §5 records as an earlier revision — `rope` as the only GPL input.
* `#adding-a-language`'s template claims: the two manifests' contents, "the
  four lines", and "**No tests**".

## Experiments

1. §14's manifest shape as one seam test, plus the two fixes the scan turned
   up: `CHANGE-core-006` (the cargo-machete table) and the stale
   `conformance-002` comment. Gate green, committed `26ea5b9`.
2. `high-level.md`'s licence prose brought to §5's position
   (`CHANGE-core-007`), plus a test comparing the three sources against each
   other. Gate green, committed `e1136f3`.
3. `#adding-a-language`'s template, as one test over every `lang_*` and
   `measure_*`, plus `CHANGE-core-008` (neither template manifest compiles as
   printed) and `CHANGE-core-009` ("nothing else changes" omits four manifest
   lines). Gate green, committed `1f758e0`.
4. Extended to `core.md#9-workspace-layout[ce5dfefab5]`, claimed and granted:
   the printed tree compared against `[workspace] members` in both
   directions, plus `CHANGE-core-010` marking the four crates phase 1a is
   forbidden to create. Gate green, committed `ac91dec`.
5. The "live once" half of §14's licence-text convention, plus `core-004`
   recording what the unresolvable assignment cost. Gate green, committed
   `d5b94e3`.

## Outcome

Confirmed, for all four sections. Five commits, five new or strengthened
scans, five Class A changes (`CHANGE-core-006` … `010`), one decision record.
Every assertion was checked with a planted mutation before being kept —
sixteen controls in all, listed in each commit message.

Not taken, and deliberately: the driver run loop, which is where the remaining
`core.md` gaps are and which shares no file with anything here; and §14's
"each `allow` carries a comment", which the §15 test already enforces more
strongly for `[workspace.lints.*]` and which §14 exempts `vendor/*` from — the
journal says why a comment-proximity scan for it would be worse than nothing.
