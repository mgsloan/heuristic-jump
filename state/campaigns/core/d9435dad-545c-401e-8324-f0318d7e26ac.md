# Campaign d9435dad-545c-401e-8324-f0318d7e26ac

- loop: core
- phase: 1a
- opened: 2026-08-05T00:45:16+00:00
- commit at open: 7901ef1bf38e49e281881f59abc394345e35295c
- session: `claude --resume d9435dad-545c-401e-8324-f0318d7e26ac`

## Target

Assigned, both taken:

- `core.md#4-project-file-enumeration[d41389f7fe]`
- `rope-modifications.md#textsummary-is-converted-too[22926927ef]`

Both turned out to be **stale**, so the campaign became the two *sections*
rather than the two gaps. Shared reading throughout:
`crates/driver/src/files.rs`, `crates/driver/tests/file_list.rs`,
`vendor/rope/src/point.rs` + `rope.rs`, `vendor/rope/tests/newtype_api.rs`,
and §4 of each document.

## Hypothesis

Both gaps were closed by commits made *after* the audit rows that opened them,
and the ordering is unambiguous rather than a judgement:

- `d41389f7fe` opened 21:12:07Z (gap-log row 16). The deletion fixture landed
  at 21:22Z, `7ba59bd`, ten minutes later; `Error::file_list_evidence` at
  `43089c3`.
- `22926927ef` opened 23:43:10Z (row 18). `CharCount` became `usize` at
  23:54Z, `831f79f`, eleven minutes later — my own previous campaign.

Neither section has been re-audited since. So the number moves for both
whatever I do, and the useful work is the claims in each section that *no test
reaches*. The bet was that a section carrying a live gap for two rounds has
more than one unheld sentence in it, and that the ones left would be the
claims whose failure is silent.

## Experiments

1. **§4's printed `TextSummary` read out of the document.** It is the one
   printed block in the section nothing parsed — the operator table and both
   signature tables already are. Two plants (a retyped field, a deleted row),
   both fired. `public_fields` gained comment-stripping so both sides read the
   same way. Green, committed `9482687`.
2. **The in-process claim, and the scope escape.** Neither held. Scoped the
   subprocess scan to the query path rather than to `driver`, because
   `shim.md` has `driver` spawning the proxied child. Three plants fired; a
   fourth was inconclusive and is recorded as such. Green, committed
   `e0e7b33`.
3. **The `InFlight { pending }` arm, and the watcher-side deletion.** No test
   ever triggered between `refresh_if_due` and `install`. Three plants, three
   correct failures on their own assertions. Green, committed `930a94f`.
4. **The debounce is not restarted.** Established by measurement rather than
   by reading: rewriting the `Pending` arm to `since: now` left all fourteen
   tests passing. The new test is the plant's only casualty. Green, committed
   `c26836e`.
5. **The no-index boundary.** Folded the two scans onto one marker walker and
   one scoping function. Plant fired. Green, committed `af17f8d`.
6. **A failed walk must not schedule a walk.** The one `ProjectError` arm no
   test reached. Plant fired. First clippy failure of the campaign
   (`redundant_clone`) — `cargo test` had passed. Green, committed `8fabf91`.

No experiment was reverted and no experiment produced no commit.

## Outcome

**Confirmed**, for both sections, and for a different reason than the
assignment supposed: both gaps were already closed, and what the campaign
actually delivered is ten tests holding claims in those two sections that
nothing reached.

Tests 319 → 329. Six commits, each plant-verified before committing.

What is *not* done, and why it is not mine: every remaining gap in the list is
in `crates/driver/src/actor.rs`, `crates/driver/src/dispatch.rs` or
`crates/shared/src/project.rs`. The planner reserved all three, and my
assignment was chosen specifically to avoid them. There was no cheap target
left that a claim would have granted.
