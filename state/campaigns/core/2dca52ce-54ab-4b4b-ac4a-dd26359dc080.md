# Campaign 2dca52ce-54ab-4b4b-ac4a-dd26359dc080

- loop: core
- phase: 1a
- opened: 2026-08-04T19:43:44+00:00
- commit at open: 692e0987fe1ab924f13f219c523271b8771c1edc
- session: `claude --resume 2dca52ce-54ab-4b4b-ac4a-dd26359dc080`

## Target

Both assigned gaps, both in `crates/driver/src/actor.rs`:

- `core.md#the-trait[5fcb043e7b]` — `actor.rs:582`. The driver synthesises
  `Stratum::Unimplemented` for a hard-capped late answer, so a real handler that
  misses its deadline writes an `unimplemented` **abstention** into §7's record —
  exactly the counter `Table::template` reads as "the template has not been
  replaced" (`measure_core/src/table.rs:158`).
- `deps.md#2-channels[8e707386b4]` — `actor.rs:203`. Nothing reads
  `Receiver::len()`. §2: with the unbounded lint withdrawn, "the only mechanism
  left is the one this section already named — log the `core` inbox depth and
  watch it".

Both verified present in this branch's `state/audit/gap-log.jsonl`, and both
fresh: `actor.rs` last changed at `d18cb36` (2026-08-03T19:05-06:00 = 01:05
UTC), and the audit stamps are 04:57/05:37 UTC, so the audit saw the current
file. No stale-gap tax this round.

**Why these together:** one file and one reading — the `select!` loop at the top
of `Actor` and the `Dispatched` arm at the bottom — and both are asserted from
`crates/driver/tests/actor.rs`, whose fixture already drives whole sessions
through `run`.

## Hypothesis

The first gap is not an open question: `state/decisions/core-017.md` is
**answered — accepted**, and its answer names this work as ordinary rather than
an escalation —

> **Work left, and it is ordinary rather than an escalation:** make the prior
> reachable without a completed outcome, and record it for capped and abandoned
> queries.

— so the campaign is core-017's option B for the case where the prior exists
(the hard cap holds the handler's `Strata` at the moment it throws the answer
away), plus reconciliation of the provisional tag.

The residue is the case where **no handler ever classified anything**: a parse
abandoned on the deadline, or a `ProjectView` read that expired before the
handler assigned a stratum. core-017 says such a query "still has a prior,
because the reference and the query are all its rule needs" — but the rule is
per-language, so evaluating it without the handler needs a seam method, and the
seam is Class B even in phase 1a. Expect to escalate that separately and tag it.

The second gap expects no design question at all: `Receiver::len()` at the one
point the loop takes an event, and a test that drives N queued events through
`run` under a capturing subscriber. `driver` may not name `tracing_subscriber`
(`seam.rs` asserts it against `deps.md` §9), so the capture is a hand-written
`tracing::Subscriber` over a `crossbeam_channel::Sender` — no lock, and the same
shape the `Checking` handler double already uses.

## Experiments

1. **`deps.md#2-channels`, the depth.** `Receiver::len()` read where the loop
   takes an event, logged only when non-empty; asserted through a hand-written
   `tracing::Subscriber` in `tests/actor.rs`, because `driver` may not name
   `tracing_subscriber` in any file. **Red once**: the seam's async scan reads
   source text, and my comment quoted the banned identifier it looks for.
   Reworded, green, committed `d1ccba7`.
2. **`core.md#the-trait`, the stratum.** `Dispatched::DeadlineExpired` carries a
   `Classified`; the hard cap fills it from the answer it drops. Found a second
   discard site the gap did not name — §8.4's conversion reads the target file
   and `ProjectView` refuses a read past the deadline, so a late answer whose
   definition is in *another* file never reaches the cap. `or_classified_by` is
   where that one picks the classification back up; both are asserted
   separately. core-017's tag reconciled, residue escalated as `core-022` with
   its consequence pinned in `pipeline.rs`. Green, committed `5e6ebb9`.
3. **Extending within `#the-trait`.** `Registry::for_path` had no caller
   anywhere, so §1's "closed files arrive as a bare path" boundary was read by
   nothing. Green, committed `ef775ad`.
4. **Extending within `#2-channels`.** §2's opening line and its one stated
   exception — `files.rs`'s two `bounded(1)` — read by nothing, and unreadable
   by a lint for the reason §2 gives. **Red once**: `std::fs::read_dir` is
   disallowed by `clippy.toml`; rewritten to follow each crate root's `mod`
   declarations the way `seam.rs` does. Green, committed `9602df2`.
5. **Claimed `core.md#two-modes-collect-and-replay[6bd547104d]`** and verified
   it **stale** — `4c50a45` (2026-08-04T04:35 UTC, before the audit stamp)
   already appends every row rather than every `CHECKPOINT_EVERY`th, so the
   partial file is a prefix and `done = rows.len()` is a sound position index.
   The code carries a comment naming the old bug. No commit; nothing to do.

Every claim above was verified by planting its negation and watching the test
fail. Six plants, six failures, all restored.

## Outcome

**Confirmed**, both assigned targets.

`5fcb043e7b`: the hard cap no longer invents a stratum. `Classified::By` carries
the `Strata` the dropped answer had assigned, so a late answer is recorded under
the stratum it was really asked about, and
`a_hard_capped_answer_keeps_the_stratum_the_handler_assigned` is the assertion
the gap asked for — "a recorded row never carries stratum_prior `unimplemented`
when the handler reported a stratum". core-017's provisional tag is gone.

`8e707386b4`: `Receiver::len()` has a caller, and
`the_inbox_depth_is_logged_when_core_falls_behind` drives four queued events
through `run` and reads `depth=3`, `depth=2`, `depth=1` off the log with the
empty inbox asserted silent.

The residue is `core-022` (open, class-b, provisional in force, tagged at
`dispatch.rs`): a query no handler ever classified still lands in
`unimplemented`, because the a-priori rule is per-language and evaluating it
without the handler needs a seam method.
`the_template_check_reads_an_abstention_no_handler_classified` holds what that
costs from the table's side, so the answer lands on something executable.

Three of the twelve listed gaps were verified stale in passing — the parse cache
(`ffcd948852`, `trees.rs` is fully `lru`-backed with both bounds), §7's "nothing
in `crates/driver` emits a record" (`bd3003d0fb`, all three particulars false
since `actor.rs`), and the claimed `6bd547104d`.
