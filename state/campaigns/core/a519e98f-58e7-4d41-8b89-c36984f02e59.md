# Campaign a519e98f-58e7-4d41-8b89-c36984f02e59

- loop: core
- phase: 1a
- opened: 2026-08-04T21:14:40+00:00
- commit at open: 661972484e4020272707a95f5788a53f313c04d4
- session: `claude --resume a519e98f-58e7-4d41-8b89-c36984f02e59`

## Target

- `core.md#4-project-file-enumeration[d41389f7fe]` — the live one, and the
  campaign's whole hypothesis.
- `core.md#the-trait[218a36571e]` and
  `core.md#two-modes-collect-and-replay[a6251d1926]` — both **stale**, re-read
  rather than assumed closed.

**Settled from `state/audit/gap-log.jsonl`, not from timestamps** (core-2's
rule). Row 15 (`187307ee`, 20:33Z) opened `218a36571e` and `a6251d1926`; my own
previous campaign 9110a409 then landed `110e108` (CHANGE-core-018, §1's seam
block) and `fd05152` (CHANGE-core-019, three subcommands), which are exactly
those two claims. Row 16 (21:12Z) audited a *different* section set and never
re-judged either section, so both rows were carried forward unjudged. Confirmed
against the section text spliced into this prompt: it already prints
`strata: Strata`, `trace: Trace`, a four-argument `decide`, and "three
subcommands". Row 16 opened `d41389f7fe` and *did* audit
`#4-project-file-enumeration`.

## Hypothesis

§4 says the editor's watcher is opportunistic and **nothing depends on it**,
and that the on-demand rescan is **the backstop that always works**. In the
code a deleted candidate is not a recall miss — it is a permanent hard failure
of every query whose candidate set contains it, with no backstop at all outside
proxy mode.

The chain: `ProjectView::scan` propagates `ProjectError::Read` on the first
vanished candidate (`project.rs:586`), `classify` makes that
`Dispatched::Failed` (`dispatch.rs:577`), and `FileListCache::observe` drops
every `Failed` as "evidence about the handler" (`files.rs:179`). Nothing marks
the list stale, so no rescan is scheduled and the next query fails identically.
`notify` is deferred, so standalone has no watcher and the failure is
permanent.

§4 also contradicts itself on exactly this point: "the one thing the on-demand
trigger structurally cannot [catch]: **deletions**" against "nothing depends on
it … the backstop that always works". Both cannot be true.

Per step 1's shared-context test the two stale assignments are taken together
with it: same document, same changelog, and the last five campaigns each found
a second defect one sentence along from a stale gap.

Expected files: `design/core.md` §4, `crates/shared/src/error.rs`,
`crates/shared/src/handler.rs`, `crates/driver/src/files.rs`,
`crates/driver/tests/file_list.rs`, `crates/shared/src/project.rs`.

## Experiments

1. `Error::file_list_evidence` in `shared` + `observe` consulting it on the
   `Failed` arm, with three tests over a real deletion and a real
   `ProjectView`. Planted first (each assertion separately). Green, committed.
2. §4's contradiction resolved as CHANGE-core-024, and `scan`'s doc comment
   given the sentence that says the failure is transient. Green, committed.
3. Re-read of `#the-trait` and `#two-modes` against the code.

## Outcome

<!-- Written at close, when the shape of the result is known. If the
     hypothesis died, say why in enough detail that nobody retries it. -->
