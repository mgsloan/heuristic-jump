# Campaign 25be160b-fd91-477b-bf7c-83df93f96491

- loop: conformance
- phase: 1a
- opened: 2026-08-03T06:33:48+00:00
- commit at open: de740edef57bcc4ea4f31c53beb46ad1e02a3ccd
- session: `claude --resume 25be160b-fd91-477b-bf7c-83df93f96491`

## Target

`core.md#82-what-replaces-it-and-why-it-is-smaller-than-it-sounds` — its one
gap: the inventory of "roughly thirty small structs" in `shared::proto`, the
read projections and the small constructed set.

Picked on the three criteria the last two campaigns used, in order:

1. **Freshness.** The §8 stamps in `state/audit/core.toml` are `04:41:48` and
   `05:10:16`, the oldest in the file — old enough that `#83` and `#87` say
   "no `proto` module exists" when `crates/shared/src/proto.rs` has held
   `PositionEncoding` and `WirePosition` for two campaigns. So part of the §8
   gap list is already answered and the rest is the inventory.
2. **Write list.** Nothing here bottoms out in a crate that does not exist:
   the whole target is one file this loop owns, which rules it in where
   `#the-dependency-graph` and the six `measure_*` sections are ruled out
   (`conformance-008`).
3. **Distance, per section rather than per gap.** §8.2 is a one-gap section,
   so it can go clean on its own — and it is the entry condition for
   `#8-protocol-types`, `#85`, `#86` and the second half of `#81`, which
   cannot be judged against an empty module.

## Hypothesis

The module is empty of messages because the previous §8 campaign stopped at
the position type, which is where §8.3's payoff is. The inventory is
mechanical rather than hard: the projections are partial structs, and the two
places judgement is required are the ones §8.5 names — the five unions — plus
§8.6's rename/default hazards.

Expected to change: `shared::proto` holds the §8.2 table's Read and Construct
rows; the read projections name the vocabulary newtypes as field types (§8.1);
untagged appears only on the kind-disjoint unions and `contentChanges` gets a
hand-written `Deserialize`; and a test scans the module's own source so the
inventory and the derive discipline are checkable rather than reviewed.

## Experiments

1. The inventory and the source-scanning test together, as one experiment: the
   test is what says which of the three lists a type is in, so writing the
   types first and the classification later would have been two passes over
   the same decision. Clippy rejected the first form of the compile-time
   `Serialize` assertion (`extra_unused_type_parameters` on a turbofish-only
   generic); rewritten to return `PhantomData<T>`. Gate green on
   `harness/gate conformance --rev HEAD`, committed `da9c815`.

## Outcome

`confirmed`. `shared::proto` holds the §8.2 table: 31 read projections, 13
constructed types, and the 5 value types that genuinely travel both ways.
`crates/shared/tests/proto.rs` (+15 tests) asserts the inventory, the read
set's absence of `Serialize`, the constructed set's absence of `Deserialize`
and the untagged allowlist against the module's own source — each of those is
a claim about a line that is *not* there, which no call-based test can make.
All three scan tests were checked for teeth by mutation before the commit.

Left deliberately, as separate campaigns: §8.5's golden corpus and `lsp-types`
dev-dependency oracle (the section's stated condition for hand-rolling, and a
corpus of captured traffic is not something a campaign can invent), and §8.6's
untrusted-document state, which needs a document map in `driver` that does not
exist yet.

**The pending-tree gate could not go green, for a reason outside this
campaign.** A human began placing `crates/similarity/` mid-session
(`conformance-008`, answered accepted), and it is untracked and denied to
every loop, so `harness/gate conformance` fails its diff-scope step on paths
the loop must not remove. Steps 1–3 pass. The commit was therefore verified
with `harness/gate conformance --rev HEAD`, which scopes the check to the
commit's own four paths and passed all seven steps.
