# Campaign 26e3bb3c-2937-495c-afea-2f1d0ae858f3

- loop: core
- phase: 1a
- opened: 2026-08-05T00:45:15+00:00
- commit at open: 7901ef1bf38e49e281881f59abc394345e35295c
- session: `claude --resume 26e3bb3c-2937-495c-afea-2f1d0ae858f3`

## Target

Both assigned gaps:

- `core.md#the-dependency-graph[a7eaf1dfa1]` — shared's authoritative list
  includes `rayon` "for `ProjectView::scan`, which executes on the pool it is
  handed at construction".
- `core.md#the-trait[218a36571e]` — `Outcome`'s arms and `CommitPolicy::decide`.

Plus, within the same two sections and the same three files, the two open
minors on them: `#the-dependency-graph`'s (`heuristic_jump` declares an unused
`tracing`; `driver` carries `lru` and `serde_json` and lacks the `rayon` and
`rustc-hash` §9's arrow lists) and `#the-trait`'s `Outcome::Committed` scan.

## Hypothesis

`218a36571e` is stale for the fourth campaign running — its audit stamp is
`20:33:33` and CHANGE-core-018 closed it nine minutes later — and the §1 block
spliced into this prompt already prints `strata: Strata`, `trace: Trace` and a
four-argument `decide`. So the cheap check is reading the gap's claim against
the splice, which costs no turns.

`a7eaf1dfa1` is fresh, opened by the audit run four hours ago. Its `found:`
proposes making the seam test "an equality against §9's list", and that
proposal is wrong: `deps.md` §14 has each dependency arrive with its first
user, so `rayon` is deliberately listed and undeclared and the equality would
fail. The real defect is one level up — the test is a *subset*, which forgives
an absence for any reason at all, so §9 can name a crate no manifest has and a
manifest can lose a crate §9 names, and neither reads differently. The fix is
to name the deferred set in the document so the difference becomes checkable.

Expected shape: doc plus changelog plus the seam manifest tests, no rename of
anything across the driver.

## Experiments

1. `heuristic_jump` drops the unused `tracing`; builds. Kept, folded into 2.
2. §9 names the chosen-but-undeclared set (`rayon` in `shared`, `rayon` and
   `rustc-hash` in `driver`), the `driver` arrow gains `lru`/`notify`/
   `serde_json`, and `seam.rs` parses the bullets and asserts
   `declared == named \ deferred` for all three crates. `project.rs`'s stale
   "§10's pool does not exist" comment corrected. Four plants, four correct
   failures. **Green, committed** (`82d2fe4`), CHANGE-core-029.
3. §9's "`measure_core` depends on `shared` and nothing else of ours" turned
   from a banned-prefix subset into an equality over the workspace member
   list. Two plants (`rope` into `measure_core`, `driver` into `measure_rust`),
   both fired. **Green, committed** (`90475a0`).
4. §9 used "ours" in two senses — the `shared` bullet's "nothing of ours" sits
   above a list containing `rope`, a workspace member — and experiment 3 had
   to pick one. §9 now says which, and says the test reads the `measure_core`
   claim more strictly than the sentence requires, and why. **Green,
   committed** (`025f56f`), CHANGE-core-030.
5. `similarity` — the crate §9 calls **frozen** — had no test on its manifest.
   First run failed with `["shared", "arraydeque", "hashbrown", "itertools",
   "smallvec"]`, which is what showed the assertion belongs over our edges
   only: `deps.md` §0 declines to settle `similarity`'s third-party set in as
   many words. The redundant `measure_rust` equality from experiment 3 came
   out in the same change. **Green, committed** (`d76b5d7`).
6. §1's "handlers must not dispatch on server identity" scanned, after
   establishing it is *not* type-held: `ServerId::KNOWN` is a `pub const` of
   the whole matrix and `from_name` is public. Plant was that comparison in
   `lang_rust`; it compiled and fired. **Green, committed** (`6fa29fb`).
7. §1's "`AbstainReason` carries no resolution vocabulary" — the existing
   printed-block comparison reads variant *names* only, so both revisions §1
   says were rejected would have passed. Allowlist scan over payload types,
   two guards, allowlist plant fired. **Green, committed** (`e124dc1`).
8. §1's "lookup becomes pointer comparison", which `vocabulary.rs` contradicts
   deliberately and a prior campaign had already corrected — in the other
   section that states it. **Green, committed** (`c616046`), CHANGE-core-032.

## Outcome

**confirmed**, for both assigned targets, though only one of them was work.

`218a36571e` was stale as predicted and cost one turn to establish; both of
`#the-trait`'s minors were closed too — the private `ServerProfile` field is
what §1 now prints, and the `Outcome::Committed` scan already existed at
`shared/tests/handler.rs:332`. So the section needed nothing, and what the
campaign did there instead was work the section rather than the gap: three
further §1 claims that no test reached, two of them about the frozen seam.

`a7eaf1dfa1` is closed by naming the deferred set rather than by declaring
`rayon` or by making the equality the audit proposed, and CHANGE-core-029 says
why in the terms the audit would have to weigh: building the fan-out to match
the document trades off `CLAUDE.md`'s rule that optimisation waits for a
benchmark, where correcting the tense trades nothing.

Seven commits, all green, 319 → 322 tests, three spec-changelog entries.

The recurring shape across five of the seven: **a claim checked as a subset
where the document states an equality.** §9's dependency list, `measure_core`'s
"nothing else of ours", `similarity`'s frozen edge, and `AbstainReason`'s
payload types were all guarded by something that could only catch an addition,
never a removal or a substitution. In each case the forgiving direction was
deliberate and documented, and in each case the document had never been asked
to say *how much* it forgave.
