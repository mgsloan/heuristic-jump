# Campaign 340b4361-1c73-4b85-a5c8-0de27852ea8d

- loop: core
- phase: 1a
- opened: 2026-08-04T21:14:40+00:00
- commit at open: 661972484e4020272707a95f5788a53f313c04d4
- session: `claude --resume 340b4361-1c73-4b85-a5c8-0de27852ea8d`

## Target

Assigned:

- `deps.md#14-workspace-cargotoml-shape[d822e97954]` — the `cargo-deny` config
- `deps.md#14-workspace-cargotoml-shape[7d21b547b7]` — `[profile.dev.package]`

Both close in `Cargo.toml` + `crates/driver/tests/seam.rs`, which is the shared
context the planner named.

### What the staleness check said, before any work

`state/audit/gap-log.jsonl`: both were opened by the `05:37` run against
`dd846068`, and that run's `sections_audited` names
`deps.md#14-workspace-cargotoml-shape`. So both were real judgements about the
tree as of that commit rather than rows carried forward by a partial audit.

- **`7d21b547b7` is closed.** `c9e5423` ("the profile takes exactly §14's list,
  and the lockfile is what says so") bumps all five packages — `tree-sitter`,
  `serde_json`, `proc-macro2`, `quote`, `syn` — and
  `every_package_section_14_names_gets_its_opt_level` holds both directions plus
  graph membership. The comment the auditor quoted (cargo rejects an override
  for a package not in the graph) is gone, and the test's doc comment records
  that cargo only *warns*.
- **`d822e97954` is real and is blocked.** `core-023` is **answered: accepted
  A — adopt `cargo-deny`**, and its ruling assigns the work to a human: a root
  `deny.toml` no loop owns, wired into a check path `harness/` denies. Both
  halves re-measured this campaign: `harness/hj check-scope core` still reports
  `deny.toml: outside core's owned paths`, and `cargo deny` is still
  `error: no such command`. §14 therefore cannot go clean this phase and the
  number will not move on it.

## Hypothesis

The assignment's number is unreachable, so the honest target is the section
rather than the two gaps: **§14 has claims with no mechanism that the auditor
could not see, and two answered decision records still carrying provisional
tags in the file the assignment hands me.** Everything below is in
`Cargo.toml`, `crates/driver/tests/seam.rs` and `vendor/*/Cargo.toml` — the
reading is already paid for.

Taken:

1. §14's "**Each `allow` carries a comment saying why.**" Nothing reads it.
   `the_workspace_lints_are_the_ones_section_15_prints` compares names and
   levels between §15's printed block and the manifest and drops trailing
   comments on both sides, so an `allow` whose argument is deleted passes.
2. §14's "**each vendored crate carries its own `[lints]` table — empty, or near
   enough**". `the_workspace_lints_reach_our_crates_and_not_the_vendored_ones`
   asserts the *non*-inheritance and nothing asserts the table that records it
   as deliberate; `vendor/sum_tree`'s empty `[lints]` is a comment and a header
   that can be deleted with nothing going red.
3. Reconciling `core-021` (closed as duplicate) and `core-023` (accepted), both
   still tagged `provisional` at `crates/driver/tests/seam.rs`.

## Experiments

Eight commits, all green, one reverted assertion. 292 → 294 tests.

1. `4bb0a60` — §14's `allow`-comment convention. New test, quantified over the
   root manifest and every member, requiring the governing comment run to name
   the lint. Four allowances across two manifests. One vendored comment
   reworded to name `unexpected_cfgs` rather than "this lint". **Committed.**
2. `ab9aadd` — the vendored `[lints]` table asserted as the record it is, in
   the test that already held its other half. **Committed.**
3. `2cd2bb6` — `core-021` and `core-023` reconciled; both provisional tags off,
   neither test deleted, and the ruling's one false premise recorded in
   `core-023` rather than edited into its argument. **Committed.**
4. `171e744` — §14's file tree read as the licence table it is. **Committed.**
5. `f285b77` — §14's toolchain pin and the vendored manifests' pointer at
   `vendor/README.md`, both ends held. **Committed.**
6. `72c95a2` — CHANGE-core-025, §6's grammar ban. One assertion written and
   **reverted inside the experiment**: `shared` re-exporting `Language`, `Tree`
   and `InputEdit` is held by the compiler (`driver/src/trees.rs` imports all
   three), so the plant failed the build rather than the test. Committed
   without it, with the fact recorded in the bullet.
7. `038413a` — CHANGE-core-026, §5's "four patches", plus the inequality that
   is the direction with teeth. **Committed.**
8. `4e9659d` — the abbreviated upstream revision held to the recorded sha, in
   all three places it is written short. **Committed.**

Twelve plants across the eight, twelve correct failures, plus the one that
failed the build instead and took its assertion with it.

## Outcome

**partial.** Both assigned gaps are accounted for and neither moved the number,
which was determined by the assignment rather than by the work:

- `7d21b547b7` — **already closed** by `c9e5423`, before this campaign opened.
- `d822e97954` — **blocked, and not by this loop**. `core-023` is answered in
  favour of adopting `cargo-deny`; the file and the check path are a human's.
  `deps.md#14-workspace-cargotoml-shape` cannot go clean this phase.

What did move: §14 has a mechanism for every claim in it that a loop can reach,
`deps.md`'s §5 and §6 minors are closed with Class A records, and the loop's
provisional-decision count went 3 → 1.

**What the next campaign should pick.** Not `deps.md`. Its only live gap is
`d822e97954` and its remaining minors need other files —
`measure_core/src/corpus.rs` (§13's `verify_checkout`),
`measure_core/src/measure_core.rs` (§9's missing log prefix), and §0's
`tempfile` row, which turns on `clippy.toml` and is denied to every loop. The
one gap in this document set that looks live is
`core.md#4-project-file-enumeration[d41389f7fe]`, in `files.rs` / `project.rs`
/ `dispatch.rs` — a whole campaign's reading, and none of it shared with this
one.
