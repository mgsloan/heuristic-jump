# Campaign ef46c8da-5215-43a6-8c6d-d20d6b57697c

- loop: core
- phase: 1a
- opened: 2026-08-04T21:14:40+00:00
- commit at open: 661972484e4020272707a95f5788a53f313c04d4
- session: `claude --resume ef46c8da-5215-43a6-8c6d-d20d6b57697c`

## Target

Assigned: `deps.md#2-channels[8e707386b4]` and
`deps.md#10-errors-one-enumerated-type-no-anyhow[d50e2285d0]`.

**Both assigned gaps were already closed when the assignment was written**, and
the planner's belief that the mechanising tests were still to write is what is
wrong rather than the code. Verified in two turns:

* `8e707386b4` ("nothing reads `Receiver::len()`") — closed by `d1ccba7`, with
  `the_inbox_depth_is_logged_when_core_falls_behind` at `tests/actor.rs:502`
  asserting `vec![3, 2, 1]` over four queued events.
* `d50e2285d0` ("the deadline conversion is explicit but silent") — closed by
  `9c30b0f` at 20:41 UTC, eight minutes *after* the audit run that opened it
  (20:33 UTC). `converting_an_expiry_into_an_abstention_is_logged` at
  `tests/actor.rs:389` already carries it.

So the target is the *section* rather than the gap, per the standing method: the
defect is one step further along the same sentence.

**Taken: §10's "that conversion is explicit and logged", on the two of
`classify`'s three callers nothing asserts.** `dispatch.rs:531` claims in a
comment that "logging here covers all three callers: a parse abandoned in
`realise`, a handler's own `?` propagation through `call`, and an expiry raised
inside §8.4's conversion". Only the third has a fixture. A comment describing
behaviour nothing checks is the shape this loop keeps finding.

## Hypothesis

The two assigned gaps are stale, so the number moves for these sections at the
next audit whatever I do. What is *not* covered is the half of §10's sentence
the gap did not name: the conversion is logged on one route out of three, and
the claim that it is logged on all three — and exactly once each — lives only in
a comment. Making it mechanical means three fixtures that reach `classify` by
three different routes, against the one that reaches `hard_cap` instead.

Shared reading with the assignment exactly: `dispatch.rs`'s `classify`,
`actor.rs`, `tests/actor.rs`, `deps.md` §2 and §10. Nothing here touches
`deps.md` prose or `seam.rs`, which are worker 2's this round.

## Experiments

1. **Two fixtures for `classify`'s unpinned routes** — `expired_before_the_parse`
   (`realise`) and `propagated_from_a_read` (`call`), plus an exact zero on
   `hard_cap`'s line for all three routes. Gate green, committed (`0fab049`).
2. **Plant: the conversion log line removed from `classify`.** Failed on route 1
   with the route named. Reverted.
3. **Plant: route 1's document shrunk to `DOCUMENT`.** Failed — the parse
   completes and the *hard cap* fires instead, printing the dropped
   `Committed { locations: [] }`. This is what proves `large_document()` is
   load-bearing rather than decoration: tree-sitter's progress callback fires
   once per 100 parser operations, so a five-line fixture never observes the
   deadline and route 1 would have been silently testing route 3's mechanism.
   Reverted.
4. **Plant: `Propagating` does not advance the clock.** Failed on route 2, with
   the captured log showing the vacuity guard's own failure line. Reverted.
5. **Retag `DECISION-core-022` → `core-025`** in `Classified::strata`. Gate
   green, committed (`74e235b`).
6. **Considered and not done: a scan for §10's word "explicit"** — that the
   `Error`→abstention conversion happens at one named site. Rejected: the only
   available shape is a source scan for `AbstainReason::` outside `actor.rs`,
   and it is the kind of test that a legitimate future change repairs by
   weakening. The mechanism §10 actually relies on is already compiler-held —
   `classify` is an exhaustive match on `Error` with no wildcard, so a new
   sub-enum fails to compile until it is classified.
7. **Considered and not done: a guard on §2's withdrawn lint.** §2's one
   remaining unmechanised sentence is that `clippy.toml` no longer denies
   `crossbeam_channel::unbounded`. Measured instead of guessed:
   `Cargo.toml:182` sets `disallowed_methods = "deny"` and
   `driver/src/driver.rs:66` calls `unbounded()`, so re-adding the entry fails
   the build immediately. A test would be strictly weaker than what is there.
8. **`hj claim core.md#4-project-file-enumeration[d41389f7fe]` — REFUSED**, held
   by another worker. One turn. It was the only remaining gap sharing this
   campaign's files (`driver/src/files.rs`, `driver/tests/file_list.rs`) and the
   same `Dispatched` enum; everything else open needs fresh reading in
   `measure_core` or `shared`.

## Outcome

**confirmed.** Both assigned gaps were already closed before the campaign
started — verified rather than assumed, in two turns — so the number moves for
these sections at the next audit regardless. What this campaign added is the
half of §10's sentence the gap did not name: "explicit and logged" is now
mechanical on all three routes into `classify` instead of one, and the driver's
single provisional tag points at the accepted record rather than at its
duplicate.

The section is saturated. §2 and §10 have, between them, one unmechanised
sentence left (§2's withdrawn lint) and it is held by the compiler. The next
campaign in this area should be `core-025`'s implementation, which is a real
piece of work and is **not** this campaign: it changes `shared::Error` to carry
the strata out of `ProjectView`'s expiry and makes `stratum_prior` nullable in
`shared::record`, `measure_core`'s table and its replay — none of which was open
here.
