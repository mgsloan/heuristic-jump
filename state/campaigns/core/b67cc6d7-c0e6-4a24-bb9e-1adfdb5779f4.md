# Campaign b67cc6d7-c0e6-4a24-bb9e-1adfdb5779f4

- loop: core
- phase: 1a
- opened: 2026-08-05T01:43:10+00:00
- commit at open: 46c1c44cbf2d09f952d04bf90bfbb40ea8c5ba11
- session: `claude --resume b67cc6d7-c0e6-4a24-bb9e-1adfdb5779f4`

## Target

- `deps.md#9-logging-and-tracing[05f24850e8]` — the assignment, and the only
  open gap in the section.
- `deps.md#9-logging-and-tracing`'s audit minor (the workspace manifest's
  comment naming one installer) — same section, same claim, same files.
- `deps.md#13-explicitly-not-depended-on`'s audit minor (gix/git2) — taken
  because its `where` is `crates/measure_core/src/corpus.rs`, already open,
  and because it is the same shape of defect: a claim about our own code that
  nothing quantified over.

Not stale: the gap was opened by the most recent audit run
(2026-08-05T01:40:56Z) and the last commit to either named file was two hours
earlier. Settled in one turn with the gap-log recipe.

`core.md#the-trait[70bb31391f]` was claimed and REFUSED — another worker.

## Hypothesis

The gap names one site and one fix. But §9 states its rules in more than one
place — the section, the workspace manifest's comment, and each installer's
own doc comment — and **a rule with several copies goes wrong in the copies
nothing reads back.** So the expectation was that the bare-stderr writer is one
symptom of a section whose claims are stated three times and checked once, and
that the audit's proposed mechanism (require each installer to name
`driver::PrefixedWriter`) is too weak to hold even the symptom, because
`tracing-subscriber`'s blanket `MakeWriter` for `Fn() -> W` makes
`with_writer(stderr_for_logging)` compile in a crate that names
`PrefixedWriter` elsewhere.

## Experiments

1. `LOG_PREFIX`/`PrefixedWriter` `driver` → `shared`; `measure_core` installs
   through a `PrefixedStderr` `MakeWriter`; §9 corrected (CHANGE-core-036);
   new seam test reads the writer out of the impl. Gate green — **committed**
   (86c1735). Plant: `with_writer(stderr_for_logging)` fires it.
2. Behavioural test in `crates/measure_rust/tests/log_stream.rs` — ran the
   shipped binary over an empty corpus split, asserted the `info` default
   fired, every stderr line prefixed, the `--format json` artifact unprefixed.
   All three plants fired. **Reverted**: `core.md` §9's template must have
   **no tests**, and `seam.rs::adding_a_language_costs_the_template_and_one_line`
   enforces it. See the journal — the rule wins and the placement is dead, not
   the test.
3. `measure_core::DEFAULT_LOG_FILTER` as a named const, asserted from `seam.rs`
   against `driver::DEFAULT_LOG_FILTER`, plus a ban on an `EnvFilter` built
   from a literal. Gate green — **committed** (b9976b5). Both plants fire.
4. The writer's destination, not just its wrapper: `PrefixedWriter<Stdout>`
   would have passed. Gate green — **committed** (1d986c7). Plant fires.
   Also tried asserting §9's premise that `rope`/`sum_tree` declare `tracing`;
   **dropped** — both `use tracing::instrument`, so the plant fails the build
   rather than the test.
5. The manifest comment fixed and made to fail: the seam test requires it to
   name every installer. Gate green — **committed** (1376166). The first
   version of the comment defeated its own check (a second paragraph mentioned
   `measure_core`); caught by planting, not by reading.
6. §13's gix/git2 reason corrected (CHANGE-core-038) and its scope asserted:
   `Command::new("git")` in `measure_core` and no other member. Gate green —
   **committed** (f9a35a3). Plant fires.

## Outcome

**confirmed.** The assigned gap is closed, and the hypothesis held: every one
of the five commits landed on a *second copy* of a §9 rule that nothing read
back — the bare writer, the private filter literal, the missing destination
check, the manifest comment, and (in §13) a reason that quantified over the
whole repository. None was the site the audit named except the first.

Outside the expected reading, and the planner should know: the fix needed
`crates/shared/src/logging.rs` (new), `crates/shared/src/shared.rs`,
`crates/driver/src/config.rs`, `crates/driver/src/driver.rs`,
`crates/heuristic_jump/src/heuristic_jump.rs` and the workspace `Cargo.toml`.
`measure_core` may not depend on `driver`, so the shared wrapper had to move
somewhere both installers can reach; no `core.md` §9 graph edge was added or
edited, which is the constraint the assignment named. `crates/driver/src/trace.rs`
was listed as expected reading and turned out to hold nothing relevant — it is
`--trace`'s record writer, not the log stream.

Left deliberately: §9's "the JSONL records are not tracing output" resists a
clean mechanical check — every formulation tried false-fires on correct code
(`queries = records.len()` in the very line §7 requires). And the §8 minor
(`clippy.toml` citing lru 0.18.1 against a 0.16 pin) is behind a denied path.
