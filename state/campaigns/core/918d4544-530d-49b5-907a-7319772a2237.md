# Campaign 918d4544-530d-49b5-907a-7319772a2237

- loop: core
- phase: 1a
- opened: 2026-08-03T23:42:00+00:00
- commit at open: a8037f4167ec5ef6dcc8de74f161ed3dc4db79da
- session: `claude --resume 918d4544-530d-49b5-907a-7319772a2237`

## Target

Three claims that all land on one surface: what `shared` exports, and what the
crates below it are therefore allowed to name directly.

- `deps.md#fxhashmap-and-fxhashset-are-the-default` (unjudged). The section's
  first rule is a claim about `shared`'s export list — "**A type alias, not a
  naked import.** `shared` exports `pub type Map<K, V> = FxHashMap<K, V>` and
  `pub type Set<T> = FxHashSet<T>`, so switching hashers later is one line
  rather than a sweep, and so the choice is visible at every use site." It
  exports neither, and five files import `rustc_hash` directly
  (`shared/src/project.rs`, `driver/src/{dispatch,trees,documents,pending}.rs`).
- `deps.md#12-testing[6590573bb2]` — "the injected clock is a `trait Clock`
  with a `TestClock` impl in `shared`, not a dependency". The trait and
  `SystemClock` are in `shared/src/deadline.rs`; the test impl is not. Five
  suites hand-roll their own: four copies of `FrozenClock`
  (`shared/tests/{project,document}.rs`, `driver/tests/{deadline,snapshots}.rs`)
  and one `DrivenClock` (`driver/tests/file_list.rs`).
- `deps.md#8-parse-cache` (unjudged) — `lru`, wrapped by `driver` for a byte
  ceiling, keyed by our own types. `driver/src/trees.rs` is an unbounded
  `FxHashMap`, which is one of the sweep's use sites.

Not `core.md#vocabulary-types[fbe658c158]`: already closed at
`shared/src/shared.rs:49` and held by `seam.rs:87`. The audit stamp on that
section predates the commit.

## Why these are one campaign

Named files shared by all three: `crates/shared/src/shared.rs` (the export list
every claim is about), `crates/shared/src/deadline.rs`,
`crates/driver/src/trees.rs` (a `rustc_hash` use site *and* the parse cache),
`crates/driver/tests/seam.rs` (where a scan-shaped assertion belongs, per
51628b98).

Not taken: `deps.md#7` needs `notify` settled — different question, different
file.

## Hypothesis

All three sections are unheld rather than wrong: the design is right, the code
does something equivalent-but-different, and nothing in the build notices
because an export list, a duplicated test double and an unbounded map all
compile. Expect the work to be a sweep plus scans in `seam.rs`, not a redesign.

## Experiments

<!-- One line per experiment: what changed, what the gate said, committed
     or reverted. Reverts are results. -->

## Outcome

<!-- Written at close, when the shape of the result is known. If the
     hypothesis died, say why in enough detail that nobody retries it. -->
