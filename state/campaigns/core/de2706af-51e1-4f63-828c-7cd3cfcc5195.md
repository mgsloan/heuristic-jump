# Campaign de2706af-51e1-4f63-828c-7cd3cfcc5195

- loop: conformance
- phase: 1a
- opened: 2026-08-03T07:56:16+00:00
- commit at open: d37cb23cbd6de70f51f16c866d7b66e4a9b24cf5
- session: `claude --resume de2706af-51e1-4f63-828c-7cd3cfcc5195`

## Target

`core.md#the-dependency-graph`, all three of its gaps — `b9eac10d67` (the node
set), `7f643e614e` (`heuristic_jump` wires the languages and calls
`driver::run`), `7f3a1bb4ec` (`tracing` missing from the *authoritative*
`shared` dependency list) — and `core.md#adding-a-language`'s only gap,
`0858868078` (the "one line in `heuristic_jump`").

One sentence for why they are one campaign: every one of them is a claim about
an **edge in §9's graph**, and the edge that is actually missing —
`heuristic_jump -> lang_rust`, through a `Registry` handed to `driver::run` —
is the same edge `#adding-a-language` calls "one line in `heuristic_jump`", so
closing it closes both sections at once.

`b9eac10d67` is expected to be a re-judge: it says four node crates "do not
exist on disk", and `measure_core`, `measure_rust`, `lang_rust` and
`similarity` are all workspace members as of `7c89f8d`.

## Hypothesis

The shipped binary links no handler. `main` resolves a `Config`, logs it and
exits, so `lang_rust` is in the workspace but in nothing built for release, and
the crate whose entire job is to be "the single place the language list is
enumerated" enumerates nothing. Adding the edge is small; what makes it worth a
campaign is that the two things guarding it are unbacked — §9's printed `main`
does not compile against the code (`HandlerRegistry`, and a `Cli` that `driver`
may not name), and nothing fails when a new `lang_*` is added to the workspace
and not wired in.

Expected: `driver::run(registry, config)` exists where `shim.md` §13 puts it,
`heuristic_jump::main` is §9's `main`, and a manifest+source scan makes the next
language's missing line a failing test rather than a silent omission.

## Experiments

1. `driver::run` where `shim.md` §13 puts it; `heuristic_jump` depends on
   `lang_rust` and `shared`, and `main` builds the registry and hands it over;
   §9's printed `main` corrected to something that compiles
   (CHANGE-conformance-009); seam test requiring every `lang_*` workspace member
   in both the manifest and the registry literal. Gate green, committed
   `d89096f`.
2. `tracing` onto §9's authoritative `shared` list and `rustc-hash`/`tracing`
   onto the graph's `driver` annotation (CHANGE-conformance-010), with a seam
   test failing on any declared dependency outside the list. Gate green,
   committed `2dc3853`.

No experiment was reverted.

## Outcome

Partial, and the remainder is a human ruling rather than a campaign.

`#adding-a-language` is satisfied: the "one line in `heuristic_jump`" exists,
and `the_language_list_is_enumerated_in_heuristic_jump` makes the *next*
language's missing line a red build — the half a compiler cannot catch, since a
`lang_python` nothing names never fails to build.

`#the-dependency-graph`'s two implementable gaps are satisfied.
`heuristic_jump` depends on `driver`, `lang_rust`, `shared`, `clap`, `tracing`
and `tracing-subscriber`, and `main` is §9's `main`; `tracing` is on the
authoritative list with a test behind it. The node-set gap (`b9eac10d67`) was
stale on its face — all four crates it says are absent have been workspace
members since `7c89f8d`.

**What blocks the section: `similarity` does not depend on `shared`**, which §9
draws and asserts. `crates/similarity/**` is denied to every loop in every
phase, so the manifest is unwritable here, and removing the edge from §9 is a
spec edit toward the code on a layering claim. Escalated as `conformance-011`
with neither option in force — there is no site this loop may write that the
answer would change, so the record is the tag. The next campaign should not
retry this; it should take `#7-observability[c4505d900b]`, whose Class B seam
widening is the largest thing still purely in this loop's hands.

Two things inherited. `driver::run` returns `Ok(())` after logging: a real seam
with a stub body, which is where the transport, codec and actor land. And the
shared-dependency test is a subset assertion, not an equality — §9 lists
`rayon`, which `shared` will not declare until `ProjectView::scan` exists.
