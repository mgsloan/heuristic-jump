# Campaign 636bbd45-e572-46de-9078-ab09897c68da

- loop: core
- phase: 1a
- opened: 2026-08-04T20:36:31+00:00
- commit at open: f193039eb06723bfe0e1728bbfee5cbdca69a4dc
- session: `claude --resume 636bbd45-e572-46de-9078-ab09897c68da`

## Target

- `deps.md#8-parse-cache[fb0aa10250]` — "the cache is keyed by `(uri, version)`
  for open docs and `(path, mtime, len)` for disk files". Only the
  open-document half exists (`driver::TreeCache`); `ProjectView::parse` has no
  cache and no key, and §8 carries no CHANGE- record saying so.

The whole assignment, and one gap rather than a section: §8 has no other gap.

## Hypothesis

§8's last paragraph is present tense about a cache half that an answered human
ruling has deferred. `conformance-005` (accepted) says no cache reached through
the `Sync` `&Query`, and there is no other route to a disk-file parse —
`ProjectView::parse` is it. So the two ways out are the cache or a CHANGE-
record, and the cache is exactly what `CLAUDE.md`'s performance posture and
that ruling forbid without a corpus and a benchmark.

Expect: a Class A edit separating the halves, a changelog entry, and a test
that asserts the corrected claim positively rather than a scan for an absent
cache.

Reading: `design/deps.md` §8, `crates/shared/src/project.rs`,
`crates/shared/tests/project.rs`, `crates/driver/src/trees.rs`,
`state/decisions/conformance-005.md`, `design/open-questions.md` question 5 —
which is about the disk key and must stay open, so the edit may not settle it.

## Experiments

1. §8's last paragraph split into its two keys, CHANGE-core-017, and
   `a_second_parse_of_the_same_path_is_a_fresh_parse` in
   `crates/shared/tests/project.rs`. Gate green, committed (`eabef7d`).
2. Plant: a path-keyed read cache in `ProjectView::read`. The read assertion
   failed with "fn beta" against "struct Beta". Reverted.
3. Plant: a `(path, len)`-keyed parse cache in `ProjectView::parse` — the key
   §8 named, minus an mtime that does not move within a second. The parse
   assertion failed with `function_item` against `struct_item`. Reverted.
4. Extension attempts: `deps.md#10-errors[d50e2285d0]` and
   `deps.md#2-channels[8e707386b4]` both REFUSED by `hj claim`.
   `deps.md#fxhashmap[e83fd58b7a]` granted and found already closed — the
   alias is at `shared.rs:85` and `the_default_map_and_set_are_the_aliases_shared_exports`
   scans every workspace member for `rustc_hash` with two vacuity controls.
   No commit; a verification is not a result the number can see.

## Outcome

Confirmed for the assigned gap. §8 now separates the two cache keys: the
open-document half is `driver::TreeCache`'s `(uri, version)`, the disk-file
half names no cache and says why, citing `conformance-005` (accepted). The key
stays written down because `open-questions.md` question 5 is about whether it
is sound, and deferring the cache defers that question rather than settling it
— so nothing Class B was decided here.

The corrected claim is mechanical: the test rewrites a fixture file to a
different text of the *same length*, which is exactly the rewrite
`(path, mtime, len)` cannot see, and requires both the read and the parse to
follow it. Both halves were planted and both failed.

Nothing else in the lane was available: the two live `deps.md` gaps are held by
other workers, `#fxhashmap` is stale, and `#14`'s `deny.toml` needs a human
(`core-023`).
