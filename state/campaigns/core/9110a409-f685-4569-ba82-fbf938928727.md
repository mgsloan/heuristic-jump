# Campaign 9110a409-f685-4569-ba82-fbf938928727

- loop: core
- phase: 1a
- opened: 2026-08-04T20:36:31+00:00
- commit at open: f193039eb06723bfe0e1728bbfee5cbdca69a4dc
- session: `claude --resume 9110a409-f685-4569-ba82-fbf938928727`

## Target

Assigned three, taking two:

- `core.md#the-trait[218a36571e]` — the printed trait block still says
  `stratum: Stratum` on both `Outcome` arms and a three-argument
  `CommitPolicy::decide`.
- `core.md#two-modes-collect-and-replay[a6251d1926]` — "`measure` therefore has
  two subcommands", and `collect` is described as enumerating identifiers.

Not taking `core.md#83-the-wire-position-type-is-inert[f9ad1766b7]`: **stale**.
The gap was opened at `e23d214` (2026-08-04T04:57:16Z); CHANGE-core-007 landed
in `e60466a` at 2026-08-04T05:10Z, thirteen minutes later, and §8.3 already
carries `pub fn line(self) -> LineIndex` with §6's justification beside it.
`git show e23d214:design/core.md | grep 'pub fn line(self)'` is empty and the
working tree's is not, which is the whole check.

Shared context, which is the test for taking two: both are stale *printed*
spec in `design/core.md` against standing code that a `CHANGE-` record never
caught up with, both land in `design/core.md` plus `state/spec-changelog/core.md`,
and neither touches a code file.

## Hypothesis

Both sections are stale in the same way and for the same reason: a campaign
changed the code under an argument recorded somewhere *else* — `conformance-013`
for the seam, `core.md#the-command-line` and `data-collection.md` §2 for the
subcommands — and the prose block in the section that first printed the shape
was never revisited. Neither is a decision to make; both are already decided in
writing, by a human ruling in one case and by a sibling section of the same
document in the other. So both are Class A: transcribe the settled answer into
the stale block and record it, changing no code.

The risk to name out loud, because the loop prompt names it: rewriting the spec
toward the code is the one way of faking progress the audit cannot see. The
defence is that neither edit is *my* reading of the code — the seam entry cites
an accepted decision record that spells out "`CommitPolicy::decide` grows to
four parameters" in the human's own rationale, and the subcommand entry resolves
a contradiction internal to `core.md` in favour of the section that agrees with
`data-collection.md`. No code file is touched in this campaign.

## Experiments

1. CHANGE-core-019 — `#two-modes`: three subcommands, enumeration is its own
   stage because positions are enumerated once per repository and not per
   server. Gate green, `5386e6b`.
2. CHANGE-core-018 — `#the-trait`: the printed block re-transcribed to the seam
   `conformance-013` settled (`Strata`, `Trace`, four-argument `decide`), plus
   `ServerProfile`'s private field and the candid note on what holds "handlers
   never construct `Outcome::Committed`". Gate green, `44e5967`.
3. CHANGE-core-020 — `#83`: the block printed `#[derive(Deserialize)]` while its
   own closing paragraph required `encode`, and §8.2's third list names the type
   as one that travels twice. Gate green, `06aa6b7`.
4. CHANGE-core-021 — `#two-modes`: the header names the repository rather than
   locating it, plus `language` and `complete`. Gate green, `8cd6e45`.
5. CHANGE-core-022 — `deps.md#8`, claimed: the `(path, mtime, len)` half
   describes a cache `conformance-005` refused. Gate green, `a2a6862`.
6. `fd05152` — the mode split pinned against `clap`'s subcommand set. Both
   halves planted ("two subcommands"; `enumerate` removed) and watched to fail.
7. `110e108` — `crates/shared/tests/handler.rs`: §1's printed block held against
   `handler.rs` by name and arity. Three plants, three failures, the first
   reproducing the gap's own words.
8. `5795fbd` — §8.2's third table read back from the document. Planted by
   dropping `WireRange`. **A second assertion in the same commit was removed
   because it could not be planted**: no `BOTH` type can lose either derive
   without a compile error, since each is embedded in a type on the other list.

## Outcome

Two assigned gaps closed, one assigned gap found stale, three further defects
found by re-reading the assigned sections against the code, and four
corrections pinned by tests so the same drift fails rather than waits for an
audit. Eight commits. `deps.md#8-parse-cache[fb0aa10250]` was claimed and
granted; `deps.md#10-errors[d50e2285d0]` was claimed and refused, which is the
ledger working.

Went outside the planner's expected reading in one respect, deliberately: it
said code files were read-only for this assignment, and three commits add
tests (`measure_core/tests/pipeline.rs`, `shared/tests/handler.rs`,
`shared/tests/proto.rs`). No source file was touched. The reason is in the
journal: the repository's own answer to "how does a printed block stay honest"
is the document-as-fixture test, and a spec-toward-code correction that nothing
pins is the shape the loop prompt warns about.
