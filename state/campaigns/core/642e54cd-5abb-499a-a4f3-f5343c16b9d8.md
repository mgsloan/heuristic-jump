# Campaign 642e54cd-5abb-499a-a4f3-f5343c16b9d8

- loop: core
- phase: 1a
- opened: 2026-08-04T23:44:29+00:00
- commit at open: b1e72c84f3c106eec217bf33aaeb53a5bb98b7ac
- session: `claude --resume 642e54cd-5abb-499a-a4f3-f5343c16b9d8`

## Target

Assigned: `core.md#both-sides-are-sets[c22993d037]` and
`core.md#two-modes-collect-and-replay[a6251d1926]`.

**Both assigned gap ids are dead on arrival, established in three turns:**

- `a6251d1926` (two subcommands vs three) was closed by `5386e6b`
  (CHANGE-core-019), which landed after the 20:33 audit run that opened it.
  The section text spliced into this prompt already says three.
- `c22993d037` appears **nowhere** in `state/` — not in `state/audit/core.toml`
  (the section is `state = "clean"` with no gap and no minor) and not in
  `gap-log.jsonl`. The planner handed over an id the audit does not hold.

So the target is the two **sections**, re-read with their claims in hand:

1. `core.md#two-modes-collect-and-replay` — its recorded minor at
   `measure_core/src/replay.rs:208`: a stored oracle answer that will not
   deserialize becomes `DefinitionResult::Null` silently, so a corrupt truth
   row scores as "the oracle returned nothing".
2. `core.md#both-sides-are-sets` — two claims nothing reads: `contained` is
   "any of the shim's locations matches" and `match_top1` *implies*
   `match_contained`, and it "is reported only alongside the result count,
   since alone it is gameable". The table prints the disjoint `MatchContained`
   counter under a column headed `contained`, and prints no result count
   anywhere.

Shared context: `measure_core/src/{replay,table}.rs` and
`measure_core/tests/pipeline.rs` for both; `shared/src/agreement.rs` read once
for the predicate both turn on.

## Hypothesis

Both sections' remaining defects are in the *reporting* half of one file pair:
a replay decides what a row means (`replay.rs`) and the table decides what the
run says about it (`table.rs`). The audit's clean/gaps split on these two
sections tracks which claims a test happened to reach, not where the
under-delivery is — `#two-modes` is judged `gaps` for a spec sentence already
fixed, while `#both-sides-are-sets` is judged clean with two normative
sentences that no code implements. Expect two commits, both in `measure_core`,
neither needing a spec edit: the document is right and the code is short of it.

## Experiments

1. `cb98e68` — a truth row whose stored answer will not deserialize is refused
   (`ConfigError::AnswerMalformed`, naming `(file, offset)`) instead of read as
   `DefinitionResult::Null`. Planted the old `unwrap_or`: the test fails.
   Gate green, committed.
2. `586bda8` — `contained` becomes the ordered metric `(match_top1 +
   match_contained) / judged`, printed beside a `results` column carrying the
   mean size of the ranked list it was judged over. Two plants — the disjoint
   counter, and a result count of one per answer — each fail their own
   assertion. Gate green, committed.
3. `9e9e2b7` — the new `locations` counter is reconciled against the records
   file's `returned` column in
   `the_records_and_the_table_are_the_same_run_counted_twice`. Planted a wrong
   count: fails 28 against 14. Gate green, committed.

No experiment was reverted, and no spec edit was needed: both sections'
documents were right and the code was short of them.

## Outcome

`confirmed`, for the two sections as sections rather than for the two gap ids,
which were both dead before this campaign started.

Three commits, 294 → 296 tests. What moved:

- `#two-modes-collect-and-replay`'s only live item was its recorded minor, and
  it is closed. Its gap `a6251d1926` was already closed by CHANGE-core-019.
- `#both-sides-are-sets` was judged **clean** while two of its normative
  sentences had no implementation at all. Both now do.

A full sentence-by-sentence pass over both sections found nothing else
unheld — the driver half of `#both-sides-are-sets` (mismatch-only divergence,
rank preservation) is thoroughly covered in `driver/src/pending.rs` and
`driver/tests/pending.rs`, and `#two-modes`' remaining claims are each pinned:
byte-identical tables across runs, the frozen `lsp_latency_us`, the three
subcommands, the provenance header, replay's absent deadline.

Closed without extending: every remaining open gap needs files this campaign
never opened (`driver/src/{files,dispatch,actor}.rs`, `shared/src/handler.rs`),
which is the step-4 test failing rather than a target list running out.
