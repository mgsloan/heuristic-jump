# Campaign 32a9eaee-72c3-4048-84a9-b89fb1d6967f

- loop: core
- phase: 1a
- opened: 2026-08-05T00:45:15+00:00
- commit at open: 7901ef1bf38e49e281881f59abc394345e35295c
- session: `claude --resume 32a9eaee-72c3-4048-84a9-b89fb1d6967f`

## Target

**The assignment is stale and was settled in one turn**, so the campaign is the
claimed target instead.

Assigned:

- `core.md#84-location-is-byte-based-and-this-fixes-a-real-inconsistency[9807aee0b3]`
- `core.md#snapshots-are-o1-to-take-and-are-parsed-before-a-handler-sees-one[02c4cde612]`
- `core.md#text-and-tree-can-never-disagree[a0661e4b63]`

All three opened at `2026-08-04T23:43:10` against commit `4266031`, and the
campaign that started 79 seconds later — `2f7fcfdd`, this worker's — closed all
three by building `shim.md` §10's worker pool. The audit that produced this
round's assignment (`fe25ac7`, `2026-08-05T00:43:32`) re-audited eleven other
`core.md` sections and **none of these three**, so the rows were carried forward
unjudged rather than re-found. Ninth campaign running with a stale assignment;
`state/audit/gap-log.jsonl` settled it in one command.

Claimed and taken instead:

- `core.md#7-observability-and-the-corpus-scan[core-025+core-026]`

## Hypothesis

Two answered decisions are still tagged provisional, both in the files this
round gave me, and **the human ruling on the second says in as many words that
they are one campaign**:

> The core loop's, and it should be one campaign with `core-025` rather than
> two: both change §7's record and nothing else shares that reading.

- `core-025` — accepted **C plus B**: `Error`'s deadline expiry carries the
  strata the handler had, and `stratum_prior` becomes nullable for the residue.
  Tagged at `dispatch.rs:198`.
- `core-026` — accepted **D**: a shed query is a *disposition*, not an
  `AbstainReason`. Tagged at `workers.rs:34`.

Both land in `shared::record` plus its two producers and `Table::observe`; D
then unblocks §10's two limits, which are a refusal in front of
`Workers::dispatch` and a `core`-inbox check in `Actor::requested` — both in
files this assignment already gave me.

Reconciling an answered decision is one of `design/loops.md` §7's five progress
axes, and it is the only one besides the test count that a campaign can move
from inside.

## Experiments

1. **`core-025` option C** — the ruling's stated first step. `ProjectView` is
   instantiated per query, so it is where a handler publishes the prior
   `resolution.md` §8 assigns before the search; `HandlerError::DeadlineExpired`
   carries it out, and `classify` turns it back into `Classified::By`. Green,
   `a7c1caa`, +2 tests. Two plants, both fired. CHANGE-core-031.
2. **`core-025` option B** — `stratum_prior` and `stratum_final` become
   nullable, and the queries that leave the table arrive beside it split by
   decision. Green, `e4ec725`, +1 test net (one replaced). Two plants fired.
   CHANGE-core-033.
3. **`core-026` option D, and §10's two limits it unblocks** — a fourth
   `decision`, the in-flight cap, the `core`-inbox check. Green, `03217e1`,
   +3 tests. Three plants fired; the third exposed a flaw in my own test (see
   below) which was fixed and re-planted. CHANGE-core-034.
4. **A replay has no route to shedding.** A shed depends on machine load, so a
   replay that could produce one would have a table that moves with what else
   was running. Green, `1d74257`, +1 test. Plant fired.
5. **The stratum codec's decode side.** `EVERY_STRATUM`'s doc claimed a test
   that did not exist. Green, `2af3cb8`, +1 test. Plant fired.
6. **The two renderings agree about the queries that left the table.** Green,
   `e2a4601`, +1 test. Two plants fired.

No experiment was reverted; every one produced a commit.

## Outcome

**Confirmed.** Both answered decisions are reconciled — no
`DECISION-core-025`/`DECISION-core-026` tag remains anywhere in `crates/` — and
`shim.md` §10's two additional limits, which `core-026` was raised to unblock,
are built and tested. Six commits, 319 → 328 tests, three spec changes.

The assigned targets were **stale and cost one turn to settle**: all three
opened against a commit that predates the campaign which closed them, and the
audit that produced this round's assignment re-judged eleven other `core.md`
sections and none of these. `state/audit/gap-log.jsonl` answers that in one
command; this is the ninth campaign running to be given an assignment already
closed.

Three things worth the next campaign's attention:

* **A number this campaign chose that the design does not contain.** §10 says
  "no heuristic work while `core` is behind" and names no threshold. The
  literal reading — any waiting event — sheds ordinary editor traffic, which
  an existing test demonstrated rather than an argument. It is 4 now, matching
  §10's own "start at 4" for the other limit, and CHANGE-core-034 says plainly
  that it is this campaign's number and not the design's.
* **`Row::failed` is now structurally always zero.** A failure has no
  `Outcome` and so no strata, so it lands beside the table rather than in a
  row. The column is accurate rather than wrong — zero failures were
  attributable to that stratum — but a future campaign that wants per-stratum
  failure counts needs a handler to be able to report a stratum with an `Err`,
  which is a seam question.
* **`Answered`'s fields are public and it now has three constructors.** The
  module argues that `Answered::of` is the one place a dispatch's endings are
  classified; with `expired` and `shed` beside it that claim is held by review
  rather than by the type. A scan asserting `driver` builds no `Answered`
  literal would mechanise it, and belongs in `seam.rs`, which this round's
  assignment put off-limits.
