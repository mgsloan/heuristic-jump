# Campaign 5cc94daa-a1bb-476a-9255-e10177487c15

- loop: core
- phase: 1a
- opened: 2026-08-04T19:43:43+00:00
- commit at open: 692e0987fe1ab924f13f219c523271b8771c1edc
- session: `claude --resume 5cc94daa-a1bb-476a-9255-e10177487c15`

## Target

The five assigned manifest/scan gaps, which is the whole assignment:

- `deps.md#14-workspace-cargotoml-shape[d822e97954]` — no `deny.toml` and no
  licence check over the dependency graph.
- `deps.md#14-workspace-cargotoml-shape[7d21b547b7]` — `[profile.dev.package]`
  bumps only `tree-sitter`.
- `deps.md#licensing-our-crates-are-mit-the-binary-is-gpl[9d0b19a109]` —
  `high-level.md` still carries "rope is the only GPL input".
- `deps.md#12-testing[6590573bb2]` — no `TestClock` in `shared`; five suites
  write their own.
- `deps.md#fxhashmap-and-fxhashset-are-the-default[e83fd58b7a]` — no
  `pub type Map`/`Set`; five sites import `rustc_hash` directly.

## Hypothesis

Four of the five are stale and one is real, and the one that is real is the
only claim in the assignment that is about the *resolved dependency graph*
rather than about a file in the repository.

The four stale ones were closed by `77c3c72` (the merge of `loop/core-2` at
06:01 UTC), which lands after the 05:37 audit that opened `7d21b547b7` and
`9d0b19a109` and long after the 00:27 one that opened the other two. So the
work is to verify them against the tests that now hold them — cheap, no code —
and spend the campaign on `d822e97954`.

`d822e97954` is real because nothing in the workspace reads a *third-party*
crate's licence. `the_gpl_inputs_are_the_two_the_documents_name` scans
workspace-member manifests, which is the half §5 rules on; the half §14's last
paragraph is about is "a third arriving without anyone deciding, which is how
a licence surface grows — not by a decision but by a dependency", and a
dependency's licence is in no file this repository owns.

## Why these together

The assignment's own answer, and it holds: `crates/driver/tests/seam.rs`,
`Cargo.toml` and `design/deps.md` §5/§12/§14 are the reading for all five.

Two more were claimed and granted after the assignment was done, on the
shared-reading test rather than a lower one:

- `deps.md#2-channels[8e707386b4]` — the `core` inbox depth is never read.
  Same document, same crate, and the file it names is `driver/src/actor.rs`.
- `core.md#the-trait[5fcb043e7b]` — the driver synthesises
  `Stratum::Unimplemented`. Same file, already read whole for the gap above,
  and its decision record (`core-017`) was already answered.

## Experiments

1. Verified all four stale gaps against the tests that close them, by reading
   `seam.rs` and comparing the audit stamps against `git log`.
   `the_injected_clock_is_shareds_and_only_the_tests_can_drive_it` (§12),
   `the_default_map_and_set_are_the_aliases_shared_exports` (fxhashmap),
   `every_package_section_14_names_gets_its_opt_level` (7d21b547b7) and
   `the_gpl_inputs_are_the_two_the_documents_name` (9d0b19a109) all exist and
   pass. No code needed; nothing committed for them.
2. Probed whether `deny.toml` is writable: `hj check-scope core` rejects it,
   and `cargo-deny` is not installed. Escalated as `core-021`
   (`harness-request`); provisional choice implemented as experiment 3.
3. `no_third_copyleft_input_reaches_the_dependency_graph` in `seam.rs`: reads
   the resolved graph with `cargo metadata --offline` and asserts the packages
   whose licence leaves no permissive option are exactly the copyleft
   workspace members. Control run with the predicate flipped to `Zlib` reports
   `foldhash`. Green, committed (`2667d0f`).
4. Considered and dropped: the same graph check for §13's declined list.
   `once_cell`, `lazy_static`, `memchr` and `aho-corasick` are all in the
   lockfile transitively, and §13 is a rule about what *we* declare — the
   existing manifest scan is correctly scoped and a graph-level version would
   fire on `ignore`'s dependencies. One turn, no commit.
5. `Actor::observed` + `the_inbox_depth_is_logged_when_core_falls_further_behind`:
   the high-water mark of `Receiver::len`, logged when it rises. Both plants
   fire. Green, committed (`a53fe15`).
6. `Dispatched::DeadlineExpired(LateStrata)`, carrying the handler's `Strata`
   through the cap. The test failed on the first run and that is where the
   second path was found — `encode` reads the target file, `ProjectView`
   refuses an expired read, so a cross-file late answer never reaches the cap.
   `classify_late` covers both. Green, committed (`b114f12`), with `core-024`
   for the residue.

## Outcome

**Confirmed**, three gaps closed with three commits, and four more verified
already closed.

`d822e97954` was the only real one in the assignment; the other four were
closed by the merge of `loop/core-2` (`77c3c72`, 06:01 UTC) after the audits
that opened them (05:37 and 00:27). They are verified, not re-done, and this
campaign has no commit for them because there was nothing to change. If the
auditor re-judges `deps.md#12-testing`,
`#fxhashmap-and-fxhashset-are-the-default` and
`#licensing-our-crates-are-mit-the-binary-is-gpl` it should find them clean.

The two claimed extensions both closed. `core.md#the-trait` produced the
campaign's only genuine bug find: fixing the hard cap alone would have left
every *cross-file* late answer filed under `unimplemented`, which is the
common case, and only writing the test surfaced it.

Reading stayed inside what the planner predicted for the assignment; the two
extensions added `crates/driver/src/{actor,dispatch,files}.rs`,
`crates/driver/tests/actor.rs` and `deps.md` §1/§2, all of which are `core`'s
and none of which another worker's assignment named.
