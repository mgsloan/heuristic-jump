# Campaign 5cc94daa-a1bb-476a-9255-e10177487c15

- loop: core
- phase: 1a
- opened: 2026-08-04T19:43:43+00:00
- commit at open: 692e0987fe1ab924f13f219c523271b8771c1edc
- session: `claude --resume 5cc94daa-a1bb-476a-9255-e10177487c15`

## Target

The five assigned manifest/scan gaps, which is the whole assignment:

- `deps.md#14-workspace-cargotoml-shape[d822e97954]` — no `deny.toml` and no
  licence check over the dependency graph.
- `deps.md#14-workspace-cargotoml-shape[7d21b547b7]` — `[profile.dev.package]`
  bumps only `tree-sitter`.
- `deps.md#licensing-our-crates-are-mit-the-binary-is-gpl[9d0b19a109]` —
  `high-level.md` still carries "rope is the only GPL input".
- `deps.md#12-testing[6590573bb2]` — no `TestClock` in `shared`; five suites
  write their own.
- `deps.md#fxhashmap-and-fxhashset-are-the-default[e83fd58b7a]` — no
  `pub type Map`/`Set`; five sites import `rustc_hash` directly.

## Hypothesis

Four of the five are stale and one is real, and the one that is real is the
only claim in the assignment that is about the *resolved dependency graph*
rather than about a file in the repository.

The four stale ones were closed by `77c3c72` (the merge of `loop/core-2` at
06:01 UTC), which lands after the 05:37 audit that opened `7d21b547b7` and
`9d0b19a109` and long after the 00:27 one that opened the other two. So the
work is to verify them against the tests that now hold them — cheap, no code —
and spend the campaign on `d822e97954`.

`d822e97954` is real because nothing in the workspace reads a *third-party*
crate's licence. `the_gpl_inputs_are_the_two_the_documents_name` scans
workspace-member manifests, which is the half §5 rules on; the half §14's last
paragraph is about is "a third arriving without anyone deciding, which is how
a licence surface grows — not by a decision but by a dependency", and a
dependency's licence is in no file this repository owns.

## Why these together

The assignment's own answer, and it holds: `crates/driver/tests/seam.rs`,
`Cargo.toml` and `design/deps.md` §5/§12/§14 are the reading for all five.

## Experiments

<!-- filled in as they run -->

## Outcome

<!-- Written at close, when the shape of the result is known. If the
     hypothesis died, say why in enough detail that nobody retries it. -->
