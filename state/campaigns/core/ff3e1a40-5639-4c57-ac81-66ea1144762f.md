# Campaign ff3e1a40-5639-4c57-ac81-66ea1144762f

- loop: conformance
- phase: 1a
- opened: 2026-08-03T07:14:00+00:00
- commit at open: ed450c4f453db75afd218387ad47e300c4af6ff0
- session: `claude --resume ff3e1a40-5639-4c57-ac81-66ea1144762f`

## Target

The measurement program does not exist. Every gap below is that one fact seen
from a different side, and none of them can be closed alone: every claim about
`measure_core` names the four-line `measure_<lang>` binary, which needs a
handler to pass, which is the language-crate template.

- `core.md#the-corpus-scan-is-a-separate-program[0f7976d913, f8f5fe0f33]`
- `core.md#one-measurement-library-one-tiny-binary-per-language[08201961d3]`
- `core.md#two-modes-collect-and-replay[fa96459aed, 90c8d7bd21]`
- `core.md#the-table-is-not-enough-a-replay-has-to-show-its-failures[9719618cc1]`
- `core.md#where-the-corpus-lives[5be0acce11]`
- `core.md#the-command-line[d2b8d07dc4]`
- `core.md#why-shared-is-separate-from-driver[37fa19707c]`
- `core.md#7-observability-and-the-corpus-scan[10d2239070]`
- `core.md#adding-a-language[5d89862345]`
- `core.md#what-the-templates-handler-does[f6bb5ebce8, 6f7fd3d943]`

Deliberately **not** taken, though they sit in the same sections:

- `#7-observability[c4505d900b]` — the handler-reported half of the record is
  inside the seam `state/phase.toml` freezes. Class B, and a separate campaign.
- `#the-dependency-graph[7f3a1bb4ec]` — `tracing` missing from §9's
  authoritative list is a spec question, not this work.
- `#the-oracle-is-the-server-being-proxied[8e6807da19]` — `ServerId`'s producer
  is the driver's startup, not the measurement path.

## Hypothesis

Twelve gaps rest on two directories not existing. Creating `lang_rust` (the
template, instantiated), `measure_core` (the library) and `measure_rust` (the
four lines) closes them together, and brings a grammar crate into the
workspace legitimately — which is the wall three campaigns have hit.

## Experiments

1. `shared::identifier` (the one rule, two entry points, one private
   predicate) + `crates/lang_rust/` (the template, instantiated) +
   `tree-sitter-rust` pinned at Zed's `0.24.2`. Green, committed `864661d`.
2. `Deadline::none`, `Error`'s `Config`/`Codec`/`Child` arms, `proto`'s client
   half, `FileList::paths`, `crates/measure_core/` (nine modules) +
   `crates/measure_rust/` (four lines), six tests. Green, committed `7c89f8d`.
   Three gate failures on the way, all diagnosed without a revert: `panic`
   and `expect` are denied in free `fn`s in `tests/*.rs`, `integer_division`
   fires on `len() / 16`, and the determinism test compared
   `heuristic_latency_us` — the one field §7 says a replay does not reproduce.
3. Two Class A spec fixes, both recorded: `servers.toml`'s location
   (CHANGE-conformance-007) and §8.2's Construct inventory
   (CHANGE-conformance-008).
4. One Class B escalation: `conformance-010`, whether `measure collect` gets a
   TOML parser. Provisional choice is a ~90-line reader confined to one module.

## Outcome

`partial`. Nine of the eleven targeted sections should now be clean; two are
deliberately left, and both are named in `state/findings/conformance.md`:

- `#7-observability-and-the-corpus-scan` still has `c4505d900b`, the
  handler-reported half of the record. It is inside the frozen seam and is
  Class B, which is why it was excluded at the start rather than discovered
  late. The record's *shape* is already §7's, so that campaign changes values
  rather than columns.
- `#adding-a-language` needs "one line in `heuristic_jump`", and
  `heuristic_jump::main` is still `fn main() {}` because `driver::run` does not
  exist. That is `#the-dependency-graph[7f643e614e]`, a different section.

See `state/journal/conformance.md`.
