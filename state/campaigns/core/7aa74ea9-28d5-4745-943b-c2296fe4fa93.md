# Campaign 7aa74ea9-28d5-4745-943b-c2296fe4fa93

- loop: core
- phase: 1a
- opened: 2026-08-04T00:35:11+00:00
- commit at open: e79ffed1ae28ccfaa9f6aea65a2c6971399547bf
- session: `claude --resume 7aa74ea9-28d5-4745-943b-c2296fe4fa93`

## Target

Assigned as one hypothesis by the planner, and they are one: every one of them
is missing because `driver::run` has no request path.

- `core.md#5-deadlines-and-abstention[f0a42a21e1]` — nothing in `src/` mints a
  `Deadline`, so "absolute, and started at request arrival" is a claim only
  tests make.
- `core.md#both-sides-are-sets[6e601d5bd1]` — `pending.rs` is complete and has
  no production caller: no `PendingQuery` is recorded, no `Resolution` is taken,
  no `ShowMessageParams` is constructed outside tests.
- `core.md#7-observability-and-the-corpus-scan[10d2239070]` — no query emits a
  JSONL record, because nothing in `driver` has both answers.
- `deps.md#11-cli-parsing-clap[521f7f6b96]` — `--trace=<path>` is absent, and
  adding the flag alone would leave it silently writing nothing.

Shared reading: `crates/driver/src/{driver,config,dispatch,documents,files,
pending,trees}.rs`, `crates/shared/src/{deadline,handler}.rs`,
`crates/measure_core/src/{record,replay}.rs`, `design/core.md` §5–§7,
`design/shim.md` §4, §7, §13.

## Hypothesis

Every module `core` would own exists and says so in its own header —
`documents.rs`: "what is missing is the actor and the transport";
`trees.rs`: "what is missing is the channel"; `files.rs`: the same. The four
gaps are one missing owner, not four missing mechanisms, so one actor closes
all four and no part of it is speculative: each of its fields is a type that
already exists and already documents which method the actor was going to call.

The transport is *not* part of this. `shim.md` §2's codec, the child spawn and
the forwarding path are a separate and much larger piece of work, and they are
in a document this phase does not audit. What this campaign builds is the
state and the transitions; what stays missing is the wire that feeds them.

## Plan

1. Move `measure_core::record` to `shared::record`. `deps.md` §9's graph
   forbids `driver -> measure_core`, and §7 requires one record shape with two
   producers, so the type has to sit where both can reach it. `shim.md` §13
   keeps the *writer* in `driver` (`report/trace.rs`).
2. `driver/src/actor.rs` — `shim.md` §13's `actor.rs`: the state `core` owns
   and §7's four-step flow over it. It mints the deadline at arrival, records
   the pending query, dispatches, and resolves against the child.
3. `driver/src/trace.rs` — §7's JSONL writer, and the half-record a proxied
   query holds until the child answers.
4. `--trace=<path>` through `Cli` -> `Config` -> the actor.

## Experiments

1. `bafcafd` — `measure_core::record` moves to `shared::record`, plus
   `Answered::of` so both producers classify a dispatch's three endings once.
   Green, committed. No gap closed on its own; nothing else could start.
2. `0c30ed4` — `actor.rs`, `trace.rs`, `--trace=<path>`, and `driver::run`
   driving an actor. Seven tests. Green, committed. This is the campaign.
3. `7ca1b17` — `--trace` through the artifact: the flag opens its sink, the
   absent flag writes nothing, an unwritable path is refused at startup. Green,
   committed.
4. `9ef5717` — a handler-reached abstention (§7's third decision), the loop's
   `select!` driven over its channel, and `core-017`. Green, committed.
5. `4cb6dad` — a `didOpen` must forget the parse cache, found by re-reading
   `notified` whole rather than by a test. Green, committed.

Two mutations run deliberately, to check the controls: minting the deadline
from `clock.now()` instead of `arrived` fails exactly the §5 test; writing the
trace row at the shim's answer instead of the child's fails three §7 tests.
Both reverted.

## Outcome

**Confirmed.** All four assigned gaps are closed by code with tests that fail
when the claim is broken, and the whole of what remains is the transport, which
is deliberate and is `shim.md`'s.

The one thing to know before picking this up: §7 has a *second* open gap
(`[c4505d900b]`, the stratum columns), so the section does not go clean on this
campaign alone. And §7's coverage denominator has a hole this campaign could
not close honestly — `hard_cap` drops a late answer's strata — which is
`core-017`, open, tagged at the one site that invents a stratum.
