# Campaign 11b9c019-6714-4563-a97b-fd9a00c5819f

- loop: harness
- phase: 1a
- opened: 2026-08-03T23:38:50+00:00
- commit at open: a8037f4167ec5ef6dcc8de74f161ed3dc4db79da
- session: `claude --resume 11b9c019-6714-4563-a97b-fd9a00c5819f`

## Target

Section 15's cost accounting: three sections that are one piece of work.

- `loops.md#the-unit-of-accounting-is-the-campaign` — the row shape.
- `loops.md#budgets-at-three-scopes` — per campaign, per phase/language, global.
- `loops.md#cost-per-unit-of-progress` — the derived number, which section 15
  says is tokens per gap closed for a conformance phase.

Nothing is judged yet, so these are unjudged sections rather than open gaps.
They share their files exactly: `harness/adapter`'s `cost` subcommand (the only
place that knows the runner's output shape), `hj`'s `cmd_cost` and phase
parsing, `harness/loop`'s pre-campaign checks, and `dashboard/serve`'s
`panel_cost`. Reading any one of them means reading all four.

## Hypothesis

The cost machinery was built to the *shape* of section 15 and stops short of
three specific claims in it.

1. The row is missing `experiments (committed / reverted / empty)` and
   `gate seconds`. Both are named in the row spec; neither is in
   `state/cost/core.jsonl`. The experiment mix is the health signal the section
   says it keeps — "a campaign that is mostly empty experiments is stalling
   regardless of what it spent" — and nothing computes it.
2. Only the first budget scope exists (`budget_usd` per loop, passed through to
   `--max-budget-usd`). There is no per-language and no global ceiling, so
   "each stops and reports rather than continuing quietly" holds for one of
   three.
3. The derived number on the dashboard is output tokens per *campaign that
   progressed*. Section 15 asks for tokens per **gap closed** at 1a and 2b. The
   gap ledger already computes `gaps_closed`, so the specified ratio is
   available and is not the one being shown.

Expected change: all three are computable from what is already recorded, with
no new data collection, and the 30 existing campaign rows are backfillable from
their teed transcripts.

## Experiments

<!-- One line per experiment: what changed, what the gate said, committed
     or reverted. Reverts are results. -->

## Outcome

<!-- Written at close, when the shape of the result is known. If the
     hypothesis died, say why in enough detail that nobody retries it. -->
