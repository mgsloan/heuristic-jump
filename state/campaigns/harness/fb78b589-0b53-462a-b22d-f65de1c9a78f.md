# Campaign fb78b589-0b53-462a-b22d-f65de1c9a78f

- loop: harness
- phase: 1a
- opened: 2026-08-05T00:26:52+00:00
- commit at open: 06e5327586d669b6d94a581568947dc270dfef02
- session: `claude --resume fb78b589-0b53-462a-b22d-f65de1c9a78f`

## Target

The two dimensions §10 and §11 say the metrics row has and it does not:

- `loops.md#10-objectives-phases-and-the-frontier[1ccaa8e7c4]` — "metrics
  still per (language, server)". The row carries no server dimension, so two
  servers' runs for one language land in one indistinguishable series.
- `loops.md#11-size-and-loc-as-objectives[c3735e6f6d]` — phase 2a's standing
  ceiling is on `measure_<lang>` size **and on per-query work**. Only the
  size half is ever observed; `guardrail_scopes` hardcodes `query_micros` to
  a reason string and reads no number, so it cannot fire.
- `loops.md#several-servers-do-not-mean-several-loops` (unjudged) — the
  section that decides what the server dimension is *for*: one frontier per
  language, still 2D, over the positions where every server agrees; a
  server's own numbers reported beside it, and "never average the two".

Shared context, concretely: all three are the shape of the row `cmd_record`
writes. `replay_fields` (hj:2687) builds the replay half, `guardrail_scopes`
(hj:6734) reads that same row back for the ceiling, and `frontier_axes_of`
(hj:6205) reads the `strata` map both of them touch. §10 and §11 are adjacent
and cross-reference; `#several-servers-…` is a subsection of §10.

## Hypothesis

Both gaps are the same defect at different ends of one pipe: a number that
`replay_digest` already computes and nothing reads (`query_micros`), and a
dimension `measure replay --server` already takes and the row already drops
(the server).

Neither needs a corpus. `replay_digest` writes `strata[*].latency_p99_us`
today, which is exactly "per-query work" — so the guardrail's observed side
is a lookup in the history it already reads for `measure_binary_bytes`.
And `measure_core::table::Report` does not echo the server it was replayed
against, so the server must be supplied to `record` the way `--replay-seconds`
is: `measure_core` keeps the invocation out of the report on purpose.

The thing to be careful about is what the frontier does with several servers.
`#several-servers-…` forbids the obvious answer — merging or averaging two
servers' strata into one number is precisely what it says never to do — and
the agreement subset it *does* want cannot be computed from aggregate
per-server reports, only from the positions, which is `measure_core`'s. So
the expected shape is the `gate-select` shape from `68b83370`: build every
consumer, and refuse honestly, naming the producer that is missing.

## Extended to

Four more, each reached from code already open. Named with what they share,
since that is the test:

- `loops.md#what-is-on-it[b7e664240e]` — §16's metrics panel wants "the
  per-stratum table — one per server where a language has several — current
  versus the recorded baseline, and the held-out verdict". Only the frontier
  was there, and the per-server half was *unbuildable* until the row gained a
  server dimension an hour earlier. Same file as the frontier panel edited
  for the first target.
- `loops.md#what-cannot-be-measured-in-isolation[33367e166f]` — the work
  counters, which are `replay_digest`'s output, the function §11's guardrail
  reads. Class A contradiction found and fixed; the gap itself escalated.
- `loops.md#mechanics-isolation-in-four-layers` — not the section's open gap
  but the outstanding half of the answered record `harness-011`, reached
  because the new `check_one_loop_per_language` had to decide whether to read
  `PINNED_HARNESS` or `HARNESS` and the documents did not say.
- `loops.md#what-this-retires[599257b098]` — the cost-phase frontier's local
  slope. `frontier_points`, `stratum_mean` and the `binary` field, all read
  this campaign.

## Experiments

Seven commits, no reverts. `hj selftest` 139 -> 151.

1. `dd688b1` — `guardrail_scopes` reads the worst stratum's p99 out of the
   metrics history, and `record` warns from the row it just built. Green.
2. `528c653` — the replay arguments become repeatable and take a `<server>=`
   prefix; the row gains `servers`; the frontier refuses to synthesise a shared
   surface from two of them. Green.
3. `5cc0487` — gate step 7 fails if two loops declare one language. Green.
4. `741634d` — the metrics panel's three missing blocks, and the first test
   coverage `dashboard/serve` has ever had. Green.
5. `817b0cf` — CHANGE-harness-014 and `harness-012`. Green.
6. `3b089a2` — `CANDIDATE_TREE_CHECKS`, the check holding it to the source, and
   the §13 and readme text `harness-011` asked for. Green.
7. `c4de65a` — `hj exchange-rate`. Green.

## Outcome

`confirmed`. Both opening gaps closed, the unjudged section closed, and four
extensions.

The hypothesis held in both halves and the second half is the one worth
recording. The guardrail was a one-function change because `replay_digest`
already computed the number — the audit's `found:` line said as much and it was
accurate. The server dimension was larger than expected not because the
plumbing was hard but because `#several-servers-do-not-mean-several-loops`
decides three separate things and each one had to be found in the prose: which
surface the frontier reads, what a single-server language does, and that a
language is one loop. The third was not a row-shape question at all and became
a gate check.

The refusal is the deliverable in the two-server case. Nothing here can compute
the agreement subset — it is a join over positions, which `measure_core`
enumerates once per language for exactly that reason — so a row replayed
against two servers with no agreement report carries no frontier position, and
three consumers say so instead of going quiet.
