# Campaign 3e637dcd-7552-460c-8eb4-fb41941ef14b

- loop: harness
- phase: 1a
- opened: 2026-08-04T19:42:23+00:00
- commit at open: 692e0987fe1ab924f13f219c523271b8771c1edc
- session: `claude --resume 3e637dcd-7552-460c-8eb4-fb41941ef14b`

## Target

Four gaps, three sections, all of them prose in `design/loops.md` that was
never reconciled with a later section of the same document:

- `loops.md#1-current-state-and-what-it-forces[7442f848b8]` — "There is no
  code."
- `loops.md#2-two-loops-two-oracles[71abe48001]` — the conformance loop's
  concurrency cell says "one writer"; §13's Workers subsection sanctions N.
- `loops.md#8-sequencing-and-gates[5a619613a3]` — 1b's gate requires the
  tune / select / **final** split decided; §12 argues the final carve-out is
  deliberately deferred.
- `loops.md#8-sequencing-and-gates[59c352d1d4]` — 1a "explicitly not the
  actor, dispatch, ..."; `crates/driver/src/actor.rs` and `dispatch.rs`
  exist.

The shared context is exact: one document, one class of defect, and the
sections each gap must be reconciled *against* — §12, §13's Workers, §18 —
are read once and serve all four. `state/phase.toml` is the second shared
read, because it is the human-owned desired state that two of these gaps
contradict.

The previous campaign (8564e2f1) named §1, §2 and §8 as its explicit
handover and deliberately did not take them: it was the campaign that
rewrote `spec_drift`, and making the detector's own campaign the first thing
it flags is a reviewer's afternoon for no gain. This is the campaign that is
not editing the detector.

## Hypothesis

Every one of these is an **internal** contradiction, not a case of the
implementation being wrong, and each resolves in favour of the later, more
argued section without trading anything off. No code changes at all; the
deliverable is four Class A spec edits plus the changelog entries that make
them reviewable.

The risk this campaign is deliberately running toward is the one §19 calls
the failure with the thinnest defence — moving the spec removes the gap from
the instrument that would have reported it. The mitigation is that each edit
must be justified by a *quoted second passage in the same document* (or by
`state/phase.toml`, which no loop may write), never by "the code does X".
The one gap where that mitigation is weakest — 59c352d1d4 — gets said out
loud in its changelog entry rather than smoothed over.

## Experiments

Ten, ten commits, no reverts.

1. §1 stops carrying an inventory of what exists. Green.
2. §2's concurrency cell becomes N workers. Green.
3. §8: 1b's gate is tuning/held-out; the registry is 1a's; the actor escalates
   as `harness-007`. Green.
4. `tagged_sites` stops reporting the selftest's own fixtures. Green, +2 tests.
5. §5 and §15 reconciled to `harness-003`; §13 layer 3 to `harness-002`. Green.
6. §6's escalation batch trigger — `hj escalations`. Green, +3 tests.
7. The findings digest crosses loops, asymmetrically. Green, +2 tests.
8. §13's merge cadence is per campaign, not per commit. Green.
9. §7's test-churn flag and done-signal cap. Green, +4 tests.

The target grew twice, both times on the shared-context test rather than on
convenience. Experiments 4 and 6 came out of the provisional-tag machinery
that experiment 3 put me in; 7, 8 and 9 came out of §13's Workers subsection,
read in full for experiment 2 and load-bearing for all three.

## Outcome

**partial**, and the partial is one gap: `loops.md#8-sequencing-and-gates`
still carries `59c352d1d4` — 1a excludes the actor, standalone and divergence
reporting, and the tree contains all three.

That gap could have been closed by deleting two words, and was deliberately
not. It is `state/decisions/harness-007.md`, Class B, no provisional choice
taken: the exclusion list stands as written and §8 now names the seam, names
the three files that cross it, and says the crossing code is covered by no
document the phase 1a audit reads. All three available options trade something
— widening §8 legalises work already done and leaves a `shim.md` surface
unaudited for a phase; adding `shim.md` to the core loop's `docs` is a
denominator change mid-phase; narrowing the code is a per-site judgement in a
crate this loop may not write.

Everything else landed. What the next campaign should not pick up: §11's
tokei gap and §7's decision-resolution and test-deletion gaps are all **stale**
— verified fixed in the tree, and the audit that lists them predates the fix.
