# Campaign 68b83370-8fef-4b7d-8ad1-13b3a3ad2b60

- loop: harness
- phase: 1a
- opened: 2026-08-04T23:50:45+00:00
- commit at open: 5aa1cdc760bf19763d18084cd4bf0848653527f3
- session: `claude --resume 68b83370-8fef-4b7d-8ad1-13b3a3ad2b60`

## Target

Three gaps that are one object seen from three sides — the frontier:

- `loops.md#the-frontier-and-why-it-stays-two-dimensional[6f1288d207]` —
  "a commit is on the frontier if no other commit beats it on every axis —
  a real, computable object". Nothing computes it.
- `loops.md#7-progress-stall-and-the-ways-it-is-faked[0adc11edaf]` —
  `cmd_campaign_close` computes four of §7's five progress terms; the
  frontier-point term is absent, and its absence is commented in the code
  as deliberate.
- `loops.md#selecting-a-version-at-a-phase-gate[dce0f16d52]` — the gate's
  three steps: compute the frontier, evaluate *those points only* on
  held-out, pick one. None of the three exists.

Shared context, concretely: all three read `state/metrics/<owner>.jsonl`
through the same `read_jsonl`/`metrics_path` pair; the §10 subsections
`#the-metrics-history`, `#the-frontier-…` and `#selecting-a-version-…` are
adjacent and define each other; and the code sites are `cmd_campaign_close`
(hj:5014), `replay_progress` (hj:4624) and the held-out block (hj:5910)
— which already carries a comment saying it stops short of the frontier.

## Hypothesis

All three are open for one reason, and it is written down rather than
inferred: `loops.md#what-is-deliberately-not-built-yet` lists "the
frontier, the evaluation half of held-out selection" as deferred, on the
argument that "a thing that only serves tuning loops is not built before
there are tuning loops". So this is not a defect anyone has missed — it is
a deferral the audit reports as three gaps because the audit judges
sections against the code and cannot see §18's exemption.

The same section says what ends the deferral: "**This list shortens as the
followup is built, and it is the list rather than the argument that
moves**", and §18 "points the conformance loop at this document precisely
so that they cross it during phase 1.5". Building the phase-2 machinery
during phase 1a is what this loop exists for.

So the hypothesis is that the frontier is buildable *now*, without a
corpus, because it is arithmetic over rows whose shape §10 specifies — and
that the half which genuinely cannot be built now is only step 2 of the
gate selection (the corpus run), which is `measure_<lang>`'s and outside
every path this loop owns. If that is right, the deliverable is: the
frontier object, the progress term that reads it, a selection command that
*refuses* to select on tuning numbers alone, and three lines struck from
§18's list.

The thing to be careful about is the progress term. Adding a disjunct to
`progress` is a metric redefinition, so it gets a rule number and a
backtest before it decides anything live.

## Experiments

<!-- One line per experiment: what changed, what the gate said, committed
     or reverted. Reverts are results. -->

## Outcome

<!-- Written at close, when the shape of the result is known. If the
     hypothesis died, say why in enough detail that nobody retries it. -->
