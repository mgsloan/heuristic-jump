# Campaign 78bbbbc4-9003-447e-9139-61389562ceb5

- loop: harness
- phase: 1a
- opened: 2026-08-04T20:14:39+00:00
- commit at open: b182ce42ce74c64391d016b25d10c2080f3b7ee9
- session: `claude --resume 78bbbbc4-9003-447e-9139-61389562ceb5`

## Target

Two open gaps and the two unjudged subsections that describe their machinery:

- `loops.md#11-size-and-loc-as-objectives[ee06332d52]` — binary size is two
  numbers, the stripped release size of `measure_<lang>` every iteration and
  the per-language link delta at phase gates. Neither is measured anywhere.
- `loops.md#12-held-out-integrity[5a507ed134]` — the loop is shown a verdict
  rather than the numbers, held-out evaluation runs at phase gates, and a
  widening gap stops the loop. The split exists physically; nothing reads it.
- `loops.md#measure_lang-size-covers-the-size-gap` (unjudged) — the proxy's
  three caveats: re-baseline when `shared`/`measure_core` move, report the
  grammar separately via `cargo bloat --crates`, the link delta stays
  authoritative.
- `loops.md#selecting-a-version-at-a-phase-gate` (unjudged) — the frontier is
  computed over the phase's commits, held-out evaluates *those points only*,
  and one is picked.

**What they share, named:** all four are the same machinery seen from two
sides — *a number computed at an iteration versus a number computed at a phase
gate*. §11's first bullet and §12's third bullet make that distinction in the
same words. The files are the same ones in every case: `harness/hj`'s metric
row (`cmd_record`, `check_test_ratchet`, `ratchet_baseline`, `metrics_path`),
`state/phase.toml`'s loop tables, and `harness/readme.md`. The sections are
`design/loops.md` §10–§12, which cross-reference each other four times.

## Hypothesis

Both gaps are in the state they are in for the same reason: the phase-gate
half of the harness was deferred wholesale as "phase 2a work"
(`harness/readme.md`'s *What is not built*), and the per-iteration halves went
with it even though they are computable today — `measure_rust` exists and
builds, and the corpus split is on disk. Expect the proxy to be a
straightforward measurement, the re-baselining to be the part with real
content, and the link delta to be blocked on `heuristic_jump` having no
per-language cargo feature — which is a gap in a path this loop may not write,
so the deliverable there is a check that says so exactly rather than silence.

## Experiments

1. `e2f6632` — the size proxy: `cargo metadata` cached once, `binary_targets`,
   `stripped_size`, `parse_bloat_crates`, `binary_size`, `hj size`, and the row
   key. Green, committed. +6 selftests.
2. **Gate went red on a check I had not touched.** `main` gained
   `campaign_process_is_running` and a selftest that reads `HJ_REPO`'s copy of
   `hj`; my branch predated both, so the *pinned* copy failed against my tree.
   Not fixable from my side — the pinned copy runs first. Merged `main`
   (stash / merge / pop, clean, ~100 lines). Green.
3. `7edacac` — the cost-phase ratchet, the re-baseline at the last commit to
   `shared`/`measure_core`, `hj check-ratchets`, and `harness-008` for the gate
   step that hard-fails in phase 3. Green, committed. +2 selftests.
4. `242b9a9` — `hj link-delta`, which exits non-zero naming the missing
   `lang-rust` cargo feature. Green, committed. +1 selftest.
5. `2062b1e` — the phase-2a guardrail: `[guardrail]` in `state/phase.toml`,
   `hj guardrail`, consulted by `harness/loop` between campaigns and by
   `record` inside one. Green, committed. +3 selftests.
6. `6dbf269` — `hj check-heldout`: root outside the checkout, `test` a sibling
   of `training`, and nothing a campaign is shown naming it. Wired into
   `harness/loop`. Green, committed. +3 selftests.
7. `387621c` — `hj heldout`: the verdict with no numbers in it, widening
   measured against the previous gate, `state/heldout/**` into `DENIED_ALWAYS`.
   Green, committed. +7 selftests.
8. `3da04c8` — `#what-is-deliberately-not-built-yet` rewritten:
   CHANGE-harness-011 for the six stale entries and the tune/select/final
   contradiction with §8 and §12. Green, committed.
9. `46c90f6` — `prompt_drift`, its dashboard card and its status line, plus
   `harness-009`. Green, committed. +1 selftest.

Nine commits, no reverts. The one red gate was experiment 2 and was not mine.

## Outcome

**`confirmed` for both targets.** §11 now measures both of its numbers, §12
implements the two structural claims and the verdict. §11's tests went 68 → 92
selftest checks across the campaign.

The hypothesis held exactly: the phase-gate half had been deferred wholesale as
2a work, taking with it per-iteration halves that were computable today; the
re-baselining was the part with real content; and the link delta was blocked on
a manifest in a path this loop may not write, so the deliverable there is a
check that names the missing cargo feature.

Two escalations, both from the same source — the code and the document disagree
about what this loop may write, in a direction that favours this loop.
`harness-008` (the gate step that hard-fails in a cost phase) and `harness-009`
(the loop may edit its own prompt; §18 says it may not). Neither was closed by
editing the document, and `harness-009` says in the record why not.
