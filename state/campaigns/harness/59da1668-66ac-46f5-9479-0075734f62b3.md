# Campaign 59da1668-66ac-46f5-9479-0075734f62b3

- loop: harness
- phase: 1a
- opened: 2026-08-04T21:18:43+00:00
- commit at open: 6d7b959f95310800cb5b31e37f58d9b956814a6b
- session: `claude --resume 59da1668-66ac-46f5-9479-0075734f62b3`

## Target

Two gaps, both of which are the reconciliation of an *answered* decision
about the same file:

- `loops.md#levers-by-which-resource-they-move[397ddee206]` — §15's first
  token lever, "keep the prompt's fixed prefix byte-identical across
  iterations". `harness-001` is `status: accepted` for option A (move the
  four per-campaign references out of the instructional body) and the
  reconciliation was never made. The gap line also names the check that is
  waiting for a home: `hj prompt-prefix` already computes the divergence
  offset and the state-block offset, so "no per-campaign value appears in
  the instructional body" is exact.
- `loops.md#mechanics-isolation-in-four-layers[bfb721b74c]` — §13's
  "`harness/` has one bounded exception … never the gate, the prompts, or
  the auditor that judge it now". `harness-009` is `status: accepted` for
  the *opposite* of the mechanical fix the audit proposes: a human ruled
  that the prompts stay writable and every revision is logged. So the
  document is what is now wrong, and the fix is a Class A reconciliation
  naming the ruling.

Shared context, concretely: both are read out of
`harness/prompts/conformance.md` and the prompt machinery in `harness/hj`
(`prompt_values`, `cmd_prompt_prefix`, `prompt_drift`,
`log_prompt_revision`, `DENIED_ALWAYS`), and both turn on the same question
— what this loop may put into, or take out of, the prompt that generates
it.

## Hypothesis

Both gaps are open because a decision was answered and nobody came back for
it. Neither needs new judgement: the ruling in each record says what the
tree should look like, and the work is applying it and leaving behind a
check that would notice it drifting back.

The second target is the one to be careful with — it edits `design/loops.md`
in the direction that favours this loop. It is only legitimate because a
human ruled it in `harness-009`; the changelog entry has to say so.

## Experiments

Five, all green, none reverted. `hj selftest` 104 → 115.

1. `10dc9b8` — the four `{{campaign_record}}`/`{{campaign_id}}` references
   move to a `## This campaign` block in the volatile tail; the opening's
   false claim about progress corrected in the same revision, which is what
   `harness-001`'s ruling asks for; `prompt_body_problems` as the exact check,
   run from `check-metrics` and scoped to `writes_harness`.
2. `18ffaf6` — §13's bounded-exception paragraph names the four files
   `DENIED_ALWAYS` actually holds, and says the live prompt templates are not
   among them, per `harness-009`'s ruling. §18's table gains the matching row.
   `check_denial_table` compares the paragraph with the constant in both
   directions. `CHANGE-harness-012`.
3. `290013a` — the campaign one-liners carry the hypothesis they opened with,
   read from the record (`hypothesis_of`), which is §14's third field and the
   only one not derivable from the session row.
4. `d8ca15d` — the 512-word digest cap becomes a number the loop is shown
   above its own digest. Reported, not gated: the digest is written after the
   last gate run of the campaign that wrote it.
5. `a836c44` — a `CLAUDE.md` change between campaigns is logged as an
   intervention, keyed on a new `constitution_sha` on the campaign-open row.

## Outcome

**confirmed**, for both targets, plus two unjudged sections closed against
claims found unimplemented while already in the file.

`loops.md#levers-by-which-resource-they-move[397ddee206]`: the instructional
body carries no per-campaign value. Measured by `hj prompt-prefix` — harness
29.2% → 47.7%, core 11.9% → 26.6%, both now diverging *after* the state block
rather than in step 1 (the percentages are against a prompt that has roughly
doubled in size since `harness-001` was written, so the absolute figures are
the comparable ones: core 4,834 → 25,734 chars).

`loops.md#mechanics-isolation-in-four-layers[bfb721b74c]`: §13 now says what
`harness-009` ruled, and `check_denial_table` holds the sentence and the
constant together. The document moved toward the code, which is the shape
being watched for, so `CHANGE-harness-012` says so and names the human ruling
that authorises it. The audit's proposed mechanical fix — deny the loop its
own prompt — is the option that ruling rejected, and was deliberately not
implemented.

`#what-a-loop-remembers-about-itself` and `#rules-are-inlined-subject-matter-is-read`
were unjudged, are implemented in the same function this campaign was already
in (`prompt_values`), and each had exactly one claim missing: the hypothesis
in the one-liner, and the `CLAUDE.md` edit reaching the log.

Checked and deliberately not taken: `#sessions-assign-the-id-own-the-transcript`
and `#reading-a-transcript` are satisfied — the adapter passes `--session-id`
and `--output-format stream-json` and tees, and the dashboard has a transcript
view — so they are clean verdicts rather than campaigns. §17's two gaps
(ccusage, EARS) are what is genuinely left in the gap list, and neither shares
anything with this campaign's reading.
