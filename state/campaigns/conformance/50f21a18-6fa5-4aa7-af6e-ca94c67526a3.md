# Campaign 50f21a18-6fa5-4aa7-af6e-ca94c67526a3

- loop: conformance
- phase: 1a
- opened: 2026-08-03T21:54:51+00:00
- commit at open: 695dff12ea3e956ff4ed12222faf0712c2138589
- session: `claude --resume 50f21a18-6fa5-4aa7-af6e-ca94c67526a3`

## Target

`core.md#vendoring-the-zed-crates[148fd8d277]` — the rope newtype sweep, which
is the whole substantive content of `design/rope-modifications.md` §4 and so of
its 20 unjudged sections.

One target, one hypothesis; the shared reading is `design/rope-modifications.md`
plus `vendor/rope/src/*`.

**Why this over the other open gaps.** Verified against the code rather than
the audit: most of the rest is already closed and waiting on a re-audit — the
two `#two-modes` gaps (`Provenance::drift` destructures every field,
`corpus::locked_grammar` reads the lockfile), `#what-the-templates-handler-does`
(`Table::template`), `#the-command-line`'s determinism gap (`table.rs` holds no
`Duration`), `#the-oracle` (`ServerProfile::proxying_named` in replay). The
three campaigns recorded `crashed` at 15:18–15:56 local committed all of that
*after* the 14:56 audit.

`#5-deadlines[f0a42a21e1]` and `#both-sides-are-sets[6e601d5bd1]` are one thing
— "the driver has no request path" — and that is `shim.md`'s transport and
actor, which `design/phases.md` assigns to phase 2b. Not taken.

## Hypothesis

The sweep is large but mechanical, and campaign 4ba19af5 costed it: ~51 public
signatures, 54 `Point::new` call sites, all 17 `ChunkSlice` public functions,
`TextSummary`'s nine fields, plus the bodies that follow. The newtypes and
their operators already exist and are inert. The 29 kept upstream tests are the
verification (§3: a mechanical diff check can no longer prove this correct).
Expect the blast radius to reach `shared`, `driver`, `measure_core` and
`lang_rust`, since every one of them names `Rope`.

## Experiments

- Read the code behind all eight `found:` clauses before picking. Six were
  already closed by three campaigns the harness recorded `crashed`. Result,
  not a commit: the measure_core cluster is a re-audit, not a campaign.
- Split the sweep line-shaped-first / byte-shaped-second, then abandoned it
  before any edit was committed: the two halves land on the same lines, so
  splitting means editing each twice. One pass instead.
- `31d2c1c` the sweep itself — `Point`/`PointUtf16`/`TextSummary` and every
  byte-shaped public signature, plus `shared`, `driver`. Gate green, all 24
  upstream rope tests pass unchanged.
- `f1a92f1` §6's signature check + §7's four-unit round trips. Mutated by
  reverting `Rope::is_char_boundary` to `usize` — which still compiles —
  and the check caught it.
- `72efa9d` the opacity scan, since nothing in the crate can fail if the
  lenient `Add<u32>` impls are added back. Mutated by planting one.
- `9ef0037` the field scan, which **failed on its first run**: public
  `TabPosition` carried two bare `usize`s. Converted. Same commit reconciles
  the five answered decisions' tag sites.

## Outcome

**Confirmed.** `core.md#vendoring-the-zed-crates[148fd8d277]` is satisfied and
with it the substantive half of `rope-modifications.md`'s twenty unjudged
sections; `vendor/README.md` records the conversion as the patch class §6 asks
for. Four commits, 193 tests, `provisional_decisions` 12 → 0.

Not taken, and why, so nobody re-derives it: `#5-deadlines[f0a42a21e1]` and
`#both-sides-are-sets[6e601d5bd1]` are one hypothesis — the driver has no
request path — and satisfying it means `shim.md`'s transport, codec and actor,
which `design/phases.md` assigns to phase 2b. That is a phase question to
escalate, not a campaign to run under a 1a prompt.
