# Campaign ec5303a7-2910-484b-9c9d-627077e6d41d

- loop: conformance
- phase: 1a
- opened: 2026-08-03T06:57:05+00:00
- commit at open: eba48b45dfd3bdb90b080a0c007b5bb47a8b2fd7
- session: `claude --resume ec5303a7-2910-484b-9c9d-627077e6d41d`

## Target

`core.md#vocabulary-types`, and its single open gap: `shared` re-exports four
of rope's seven text-shaped newtypes, so `shared::ByteColumn`,
`shared::Utf16Column` and `shared::CharCount` are not nameable by a crate that
may not depend on `rope` — which is the reason the section gives for the
re-export existing at all.

Picked by the stated rule — among gaps, the one whose section is closest to
going clean, because the number moves per section. This is the only section
with exactly one gap that is (a) in files that exist, (b) not blocked on a
crate nobody has written, and (c) not plausibly stale. `#the-trait` is also a
one-gap section but that gap is `ProjectView::{candidates, parse, scan}` — the
parse LRU and the bounded scan pool — which is both a whole campaign and
inside the frozen seam. `#87-where-it-lives` is one gap that the last campaign
believes is already satisfied by `shared::proto`, so there is nothing to
implement there; it wants a re-judge, not a campaign.

## Hypothesis

The gap is a leftover from campaign `bc8f02bb`, which targeted this section
and confirmed: the three missing names are the three the section itself
describes as the ones "handlers do not use", so they were the easy ones to
skip while writing the seam. Nothing depends on them being absent, and nothing
in `shared` has to change to add them.

The reason they went missing is that no compiler error could report it — the
re-export list is prose-checked. So the fix is two parts, and the second is
the one that lasts: add the three names, and put the assertion in a test that
lives in a crate which *does not depend on rope*, so the property under test
is the one the section states rather than a reading of the export list.
`driver` is that crate (`crates/driver/Cargo.toml` names `rustc-hash`,
`shared`, `tracing`), and `crates/driver/tests/seam.rs` already exists to hold
§1's other where-claims.

## Experiments

<!-- One line per experiment: what changed, what the gate said, committed
     or reverted. Reverts are results. -->

1. Re-export all seven from `shared`, and assert in `driver`'s seam test that
   each is nameable as `shared::…` and defined in `rope::…`.

## Outcome

`confirmed`, in one experiment with no reverts (`9e581f7`). All seven of §1's
text newtypes are re-exported from `shared`, and
`crates/driver/tests/seam.rs::the_text_vocabulary_is_nameable_through_shared_and_defined_in_rope`
holds it: the `use shared::{..}` fails to compile if a name leaves the list,
and `type_name().starts_with("rope::")` fails if `shared` ever defines one
itself instead of re-exporting it. The section's other claims were checked and
hold — `ByteRange::{contains, overlaps}`, `Location`'s private fields and sole
`at_node` constructor, `Confidence::new`'s `0.0..=1.0` and NaN refusal, the
interned `LanguageId`/`FileExtension`/`ServerId` — so the section should judge
clean rather than trading one gap for another.

The same gap is also listed under `#vendoring-the-zed-crates`, which this
closes there too; that section stays dirty on its other two, one of which is
the rope public-API newtype sweep.

The generalisable part is in the journal and the findings: a re-export list is
unfalsifiable from inside the crate that owns it, so a where-claim's test has
to live in a crate that cannot reach the type by the other path. That is why
this landed in `driver`'s tests rather than `shared`'s.
