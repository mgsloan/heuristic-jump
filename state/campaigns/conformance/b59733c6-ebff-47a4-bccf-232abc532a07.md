# Campaign b59733c6-ebff-47a4-bccf-232abc532a07

- loop: conformance
- phase: 1a
- opened: 2026-08-03T03:32:43+00:00
- commit at open: dcd244c5aaab5035ea1f03d00d04c6b4f20c2524
- session: `claude --resume b59733c6-ebff-47a4-bccf-232abc532a07`

## Target

`core.md#9-workspace-layout`.

Every section is unjudged and there are no open gaps, so "closest to going
clean" is decided by dependency rather than by distance: no other section can
be satisfied before a cargo workspace exists, because there is nowhere to put
the code. `core.md#9-workspace-layout`'s own claims — `crates/` beside
`vendor/`, the crate names, `publish = false`, `driver` rather than `core`,
`heuristic_jump` producing the `heuristic-jump` binary — are checkable against
manifests alone and need neither `vendor/rope` nor a handler.

Scope is bounded to what `deps.md` §14 says this piece of work creates:
`shared`, `driver`, `heuristic_jump`. The layout's other rows belong to
sections that are audited separately — `core.md#adding-a-language` for
`lang_rust`/`measure_rust`, `core.md#vendoring-the-zed-crates` for `vendor/`,
`core.md#the-dependency-graph` for the external edges — and `similarity` is
denied to every loop outside a whole-repository phase.

## Hypothesis

The section is unjudged because nothing exists at all. Creating the workspace
root per `deps.md` §14/§15 and the three crate skeletons should take it to
clean without touching any other section's claims. Expected friction: a
skeleton has no tests, and `cargo nextest run` exits 4 on an empty test list,
so the commit needs one test that carries a §9 claim rather than a placeholder.

## Experiments

1. `cargo new`-equivalent probe in `/tmp`: package `bin_name` builds
   `target/debug/bin_name`, not `bin-name`. §9's "without a `[[bin]]` rename"
   is false about cargo. Fixed the document and added the rename
   (CHANGE-conformance-001). Not committed on its own.
2. Ran `harness/gate conformance` before writing anything: red at step 4,
   because `state/sessions.jsonl` and `state/interventions.jsonl` are dirty in
   the working tree and denied to every loop. Not fixable from inside the loop
   — decision `conformance-001`, and the gate is taken post-hoc with `--rev`.
3. Workspace root + `shared`/`driver`/`heuristic_jump` + the binary-name test.
   Gate green (`--rev HEAD`, 7 steps), committed as `5092517`.
4. The metrics row for `5092517` reads `provisional_decisions: 0` with one
   provisional choice in force, because the counter greps `crates` and
   `vendor` and the tag is in the root `Cargo.toml`. Written up rather than
   repaired; committed as `e37a7db`, gate green.

## Outcome

Confirmed. `core.md#9-workspace-layout`'s claims are realized in manifests and
one of them — the artifact name — is now enforced by a test rather than by
prose. Two harness-request decisions were raised and neither blocked the work:
`conformance-001` (the gate cannot be run green pre-commit while the harness's
own state files are dirty) and `conformance-002` (`deps.md` §14 requires
`LICENSE-*` and `rust-toolchain.toml` at the root, which the ownership table
does not grant, so they are omitted and `rust-version` carries the pin
instead).

`[profile.dev.package]`'s opt-level bumps are deferred, not skipped: cargo
errors on a profile override naming a package that is not in the graph, and
`tree-sitter`/`serde_json` are not dependencies of anything yet. They land
with the first crate that pulls them in.
