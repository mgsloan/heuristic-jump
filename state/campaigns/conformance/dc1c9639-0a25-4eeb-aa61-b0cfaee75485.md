# Campaign dc1c9639-0a25-4eeb-aa61-b0cfaee75485

- loop: conformance
- phase: 1a
- opened: 2026-08-03T05:10:16+00:00
- commit at open: 20054d1c5d178eb054c23ca0955c4895ace11741
- session: `claude --resume dc1c9639-0a25-4eeb-aa61-b0cfaee75485`

## Target

`core.md#3-position-encoding` — both of its open gaps, which are one piece of
work:

1. "conversion lives in one module with exhaustive property tests against a
   reference implementation"
2. "a settled `PositionEncoding` set once and never inferred, with the
   no-UTF-16-past-the-edge rule enforced by the type system: `proto::WirePosition`
   has private fields and yields a `ByteOffset` only when handed the negotiated
   encoding and the text"

Picked over the other gapped sections because it is the one whose whole gap
list is a single self-contained module, and the number moves per section rather
than per gap. The single-gap alternatives are each much larger than they look:
§8.2 is thirty structs, §8.5 needs a golden corpus and an `lsp-types` oracle,
§8.6 needs a document map in `driver`, §7 needs `measure_core`, and
§9/#adding-a-language both need `crates/similarity`, which is denied to this
loop. §3's two gaps need `rope` (present), one new module and one test file.

The section's `minor` item — `Cargo.toml:87` justifying the un-denied
`indexing_slicing` with "the protection that holds here is structural:
`WirePosition` has private fields" in the present tense about a type that does
not exist — is fixed by the same work, without editing the comment.

## Hypothesis

The section is in `gaps` because its audit predates any code at all (it is one
of the six still stamped `03:47:22`, which also claims `vendor/` does not exist
on disk). Nothing about it is blocked: `rope` already carries the
`PointUtf16`/`OffsetUtf16` dimensions and the clip functions the section names
as the mechanism, so the module is a wrapper that adds the *exactness* rope
deliberately does not have — rope clips, and a clip that silently moves a
position is the failure mode this section exists to prevent.

Expect: `shared::proto` seeded with `PositionEncoding`, `WirePosition`,
`Error::Encoding`, and a property test in `crates/shared/tests/` comparing the
conversions against a naive `&str` reference over text with astral-plane
characters.

## Experiments

<!-- One line per experiment: what changed, what the gate said, committed
     or reverted. Reverts are results. -->

## Outcome

<!-- Written at close, when the shape of the result is known. If the
     hypothesis died, say why in enough detail that nobody retries it. -->
