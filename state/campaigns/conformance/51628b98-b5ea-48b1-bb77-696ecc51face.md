# Campaign 51628b98-b5ea-48b1-bb77-696ecc51face

- loop: conformance
- phase: 1a
- opened: 2026-08-03T22:26:36+00:00
- commit at open: 7e69ccb3416db503f19442669a88929e8315c9ab
- session: `claude --resume 51628b98-b5ea-48b1-bb77-696ecc51face`

## Target

`deps.md` joined the audited documents at `dedcbd3` and nothing in it has ever
been judged. Four of its sections are one piece of reading — the workspace
manifest, the seven member manifests, the two vendored ones, and the crate
directories beside them:

- `deps.md#14-workspace-cargotoml-shape`
- `deps.md#licensing-our-crates-are-mit-the-binary-is-gpl` (§5's subsection)
- `deps.md#13-explicitly-not-depended-on`
- `deps.md#0-summary-table`

The shared files are named rather than gestured at: `Cargo.toml`,
`crates/*/Cargo.toml`, `vendor/*/Cargo.toml`, and
`crates/driver/tests/seam.rs`, which is already where a manifest-shaped claim
lives — it carries `deps.md` §12's `lsp-types` check for exactly this reason.

## Hypothesis

The manifests were written against `deps.md` and quote it in their comments,
so most of these four sections are *satisfied but unheld*: nothing fails if a
member starts naming a version inline, if `vendor/*` acquires
`[lints] workspace = true`, or if `anyhow` is declared tomorrow. The audit
cannot distinguish "true" from "true today", so the deliverable is mechanism,
not prose.

One claim is expected to be actually false. §14 says license texts "live once
at the workspace root and are symlinked into each crate", citing Zed's 245
symlinks and no regular copy. Six of seven `crates/*` have no symlink at all.

The target was extended without fresh reading: §15, §1, §12, §4, §6 and §2 are
the same five files. Ten sections in all.

## Experiments

1. **§5's licensing subsection.** Six of seven `crates/*` had no licence text
   beside them; created the symlinks and asserted both halves — the symlink is
   asserted rather than the file, since a regular copy satisfies the letter
   and fails the reason. Committed `1fd4037`.
2. **§14's four manifest conventions.** No inline versions, the dotted
   inherit form, `[lints]` in ours and not in `vendor/*`, an explicit
   `[lib] path`. Committed `64b6ee5`. The vendor half has **no negative
   control** and the doc comment says so; see the journal.
3. **conformance-014's tagged sites**, answered mid-campaign. `cc9a8dd`.
4. **§13's rejected crates**, against declarations rather than `Cargo.lock`.
   Committed `b085481`.
5. **§0's summary table**, transposed per crate, as a subset. `12bdfe1`.
6. **§15's lint block**, compared against `deps.md` itself rather than a
   transcribed constant. They agree, 45 entries each. `fd6a72b`.
7. **§1's async ban**, the source half. `e5a8503`.
8. **§12's testing table** and two of the three load-bearing pins; opened
   conformance-015 over `tempfile`. `af16bb6`, then `a467d5e` applying the
   ruling.
9. **§4's serde_json features**; filed conformance-016. `6da688f`.
10. **§6's grammar pins**, both directions. `60102de`.
11. **conformance-016's assigned edit**: §2 records the removed `unbounded`
    lint where it used to fire. `10ca2ab`.

Eleven commits, eleven gate passes, no reverts.

## Outcome

Ten sections mechanized, one real defect fixed (six missing licence symlinks),
three decision records — **all three answered within the campaign**, and every
ruling's assigned edit applied (CHANGE-conformance-015 and -016).

The finding that generalizes is about controls rather than about `deps.md`:
three of the assertions written here **cannot be made to fail**, because the
mutation is rejected before the test runs. Each is documented as such in place
rather than left to imply coverage it does not have. See the journal.
