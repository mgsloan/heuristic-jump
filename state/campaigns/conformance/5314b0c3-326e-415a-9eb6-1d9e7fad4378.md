# Campaign 5314b0c3-326e-415a-9eb6-1d9e7fad4378

- loop: conformance
- phase: 1a
- opened: 2026-08-03T07:01:56+00:00
- commit at open: e448ccb3e6b43b2b85212d7694ed9858e1b7899d
- session: `claude --resume 5314b0c3-326e-415a-9eb6-1d9e7fad4378`

## Target

`core.md#6-the-agreement-predicate`

One gap, and the only remaining one-gap section whose work is neither a new
crate nor inside the frozen seam:

* `#vocabulary-types`, `#87-where-it-lives`, `#84`, `#85` — checked the code
  first, per the standing finding. Their gaps are stale: `shared` already
  re-exports all seven of rope's newtypes (`shared.rs:38-40`), and
  `shared::proto` exists at 809 lines with `WireLocation`, `WireRange`,
  `WireLocationLink` and an untagged `DefinitionResult`. Re-targeting them is
  a re-judge, not work.
* `#the-trait` (`ProjectView::{candidates, parse, scan}`) is inside the seam
  `state/phase.toml:21-27` freezes, and is a whole campaign of its own.
* Every other one-gap section (`#one-measurement-library…`,
  `#where-the-corpus-lives`, `#the-command-line`, `#the-table-is-not-enough`)
  is blocked behind `measure_core` existing, which is one large campaign that
  would move none of them alone.
* `#6` is pure logic in `shared`, and its one dependency — a modelled
  `Location | Location[] | LocationLink[] | null` — landed since the audit
  ran. The gap's own `where:` points at `proto.rs:36` and says that type is
  missing; it is not.

## Hypothesis

The section is unimplemented rather than hard: nothing in it needs a
component that does not exist. The audit's own mechanical check — "one enum
whose `Display` is the string written to the record, so the classifier and
the metric cannot acquire separate vocabularies" — is the deliverable, and
the predicate belongs in `shared` because `#the-corpus-scan-is-a-separate-program`
independently requires it there.

Expected obstruction: §6's rows are pairwise, and neither side is one
location, so the lift is underdetermined in at least the severity column.

## Experiments

1. `shared::agreement` — `Severity`, `Agreement`, `Agreement::classify`, the
   normalisation of all four `DefinitionResult` shapes, and
   `crates/shared/tests/agreement.rs`. **Committed.**
2. `WirePosition::line` — the child's row is unreachable without it; §8.3's
   "inert" is about offsets, and a row is not one. Folded into 1.
3. `tests/proto.rs`'s source scan — it asserts `proto.rs`'s derive inventory
   against a transcribed table, so the accessor had to be an addition to an
   existing type rather than a new one. Confirmed by running it; no revert.

## Spec changes

* **CHANGE-conformance-005** (Class A) — §6 says both sides normalise to a set
  of `Location`, i.e. `(DocumentUri, ByteRange)`, and three paragraphs later
  says the predicate reads nothing. Byte space is unreachable from a wire
  range without the document's text. Resolved toward "reads nothing".
* **CHANGE-conformance-006** (Class A) — §6's table has no row for the shim
  committing nothing while the child answered.
* **conformance-009** (Class B) — what makes two files "the same module tree",
  which decides the `near_module`/`unrelated` split and so what
  `high-level.md`'s two severity budgets are measured over.

## Outcome

Confirmed. `crates/shared/src/agreement.rs` implements §6: `Severity` whose
`Display` is the exact string §7's record carries, `Agreement` with the
severity carried inside `Mismatch` so "undefined otherwise" is
unrepresentable, `Agreement::classify` as the only public entry point,
normalisation of `Null`/`One`/`Many`/`Links` to the `(uri, line)` pairs the
predicate compares, the 3-line tolerance, and the `top1`/`contained` lift.
13 tests in `crates/shared/tests/agreement.rs`.
