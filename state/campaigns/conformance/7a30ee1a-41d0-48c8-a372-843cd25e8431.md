# Campaign 7a30ee1a-41d0-48c8-a372-843cd25e8431

- loop: conformance
- phase: 1a
- opened: 2026-08-03T09:16:15+00:00
- commit at open: 813f720f24d1edf2a609e0394500cc81fae5a7bd
- session: `claude --resume 7a30ee1a-41d0-48c8-a372-843cd25e8431`

## Target

`core.md#7-observability-and-the-corpus-scan[c4505d900b]` — the handler-reported
half of §7's per-query record does not cross the seam.

The section's sibling gap `[10d2239070]` ("no record type, no serialization and
no emission site exists") is stale: `crates/measure_core/src/record.rs` is
§7's record, `replay::write_records` serializes it, and `measure_core` is a
workspace member. Closing `c4505d900b` is therefore what makes the section
clean, and it is a single hypothesis: **the record is complete in shape and
empty in content because `Outcome` carries one `Stratum` and nothing else.**

## Hypothesis

`record.rs`'s `HandlerReport` already names the exact hole and says why it is
empty — "widening `Outcome` is a Class B escalation on the frozen seam and
therefore its own campaign". This is that campaign. The work is:

1. `Strata` (prior + settled) replacing the single `Stratum` on both arms of
   `Outcome`, with `Refinement` as a two-variant enum so §7's "one refinement,
   to `AmbiguousName` or `ExternalDependency`" is a compile-time fact.
2. `Trace` carrying `margin`, `considered`, `stages`, `stage_us`,
   `bytes_scanned`, `files_parsed`, write-only from the handler's side so §7's
   "nothing branches on `stages`" survives.
3. The consumer half: `replay` fills the record from a `Trace` instead of from
   `HandlerReport::default()`, and `Table` counts coverage on the prior stratum
   and precision on the settled one, which is the whole reason §7 says the
   stratum is two fields.

Class B on the frozen seam: `state/decisions/conformance-013.md`.

## Experiments

<!-- filled in as they run -->

## Outcome

<!-- written at close -->
