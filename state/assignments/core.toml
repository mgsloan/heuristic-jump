# One round's assignments, written by `hj plan-merge` from the planner's
# answer. Regenerated every round; not history.
planned = "2026-08-05T00:45:13+00:00"

[worker.1]
targets = ["core.md#84-location-is-byte-based-and-this-fixes-a-real-inconsistency[9807aee0b3]", "core.md#snapshots-are-o1-to-take-and-are-parsed-before-a-handler-sees-one[02c4cde612]", "core.md#text-and-tree-can-never-disagree[a0661e4b63]"]
reading = "crates/driver/src/actor.rs, crates/driver/src/dispatch.rs, crates/driver/tests/actor.rs, crates/driver/tests/snapshots.rs, design/core.md \u00a78.4"
why = "all three are the same finding \u2014 dispatch/realise run on the actor thread because there is no worker pool \u2014 so they share actor.rs and dispatch.rs and cannot be split; this worker owns those two files and must not append to seam.rs"

[worker.2]
targets = ["core.md#the-dependency-graph[a7eaf1dfa1]", "core.md#the-trait[218a36571e]"]
reading = "crates/shared/src/project.rs, crates/shared/src/handler.rs, crates/shared/Cargo.toml, crates/driver/tests/seam.rs:1731-3540, design/core.md \u00a77 and \u00a79"
why = "sole holder of crates/driver/tests/seam.rs this round; both gaps are core.md-vs-crates/shared reconciliation (\u00a79's rayon list, \u00a77's Outcome/CommitPolicy block) and the fix is expected to be doc/changelog plus the seam manifest, not a rename of Strata across the driver"

[worker.3]
targets = ["core.md#4-project-file-enumeration[d41389f7fe]", "rope-modifications.md#textsummary-is-converted-too[22926927ef]"]
reading = "crates/driver/src/files.rs, crates/driver/tests/file_list.rs, vendor/rope/src/point.rs, vendor/rope/tests/newtype_api.rs, vendor/README.md"
why = "two small independent targets that touch nothing worker 1 or 2 owns; files.rs already gained Error::file_list_evidence since the audit (43089c3) so the remaining work is the deletion fixture in file_list.rs \u2014 verify before rewriting, and do not edit dispatch.rs or shared/src/project.rs"
