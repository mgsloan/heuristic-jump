# One round's assignments, written by `hj plan-merge` from the planner's
# answer. Regenerated every round; not history.
planned = "2026-08-04T21:14:39+00:00"

[worker.1]
targets = ["core.md#4-project-file-enumeration[d41389f7fe]", "core.md#the-trait[218a36571e]", "core.md#two-modes-collect-and-replay[a6251d1926]"]
reading = "design/core.md, state/spec-changelog/core.md, crates/driver/src/files.rs, crates/driver/src/project.rs, crates/driver/src/dispatch.rs, crates/driver/tests/file_list.rs, crates/shared/src/handler.rs, crates/measure_core/src/cli.rs"
why = "sole owner of design/core.md: all three open core.md claims in one hand so no second worker rewrites that doc or its changelog; its test lands in tests/file_list.rs, not seam.rs"

[worker.2]
targets = ["deps.md#14-workspace-cargotoml-shape[d822e97954]", "deps.md#14-workspace-cargotoml-shape[7d21b547b7]"]
reading = "Cargo.toml, Cargo.lock, deny.toml (absent), design/deps.md \u00a714, crates/driver/tests/seam.rs"
why = "both \u00a714 anchors close in seam.rs + root Cargo.toml, so this is the only worker holding seam.rs and it can take the section clean"

[worker.3]
targets = ["deps.md#2-channels[8e707386b4]", "deps.md#10-errors-one-enumerated-type-no-anyhow[d50e2285d0]"]
reading = "crates/driver/src/actor.rs, crates/driver/src/dispatch.rs (classify), crates/driver/tests/actor.rs, crates/driver/tests/deadline.rs, design/deps.md \u00a72 and \u00a710"
why = "both code fixes already landed (actor.rs:256 depth, dispatch.rs:548 log) so the work is the mechanising test in tests/actor.rs \u2014 deps.md prose and seam.rs stay worker 2's"
