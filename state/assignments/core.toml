# One round's assignments, written by `hj plan-merge` from the planner's
# answer. Regenerated every round; not history.
planned = "2026-08-04T20:36:31+00:00"

[worker.1]
targets = ["core.md#83-the-wire-position-type-is-inert[f9ad1766b7]", "core.md#the-trait[218a36571e]", "core.md#two-modes-collect-and-replay[a6251d1926]"]
reading = "design/core.md (\u00a78.3, \u00a76, the-trait, two-modes-collect-and-replay), state/spec-changelog/core.md, crates/shared/src/proto.rs, crates/shared/src/handler.rs, crates/driver/src/agreement.rs, crates/measure_core/src/cli.rs, crates/shared/tests/proto.rs, crates/measure_core/tests/pipeline.rs"
why = "all three are stale printed spec against standing code (line() accessor, Strata/Trace/4-arg decide, three subcommands), so all three land in design/core.md plus state/spec-changelog/core.md \u2014 sole holder of both; code files are read-only for it"

[worker.2]
targets = ["deps.md#10-errors-one-enumerated-type-no-anyhow[d50e2285d0]", "deps.md#2-channels[8e707386b4]"]
reading = "crates/driver/src/dispatch.rs, crates/driver/src/actor.rs, crates/driver/tests/deadline.rs, crates/driver/tests/actor.rs, crates/driver/tests/seam.rs"
why = "both are driver-internal logging with one reading (dispatch.rs:528 silent deadline\u2192abstention, actor.rs:203 inbox depth); sole holder of seam.rs this round, and \u00a72 is likely already closed at actor.rs:256 \u2014 verify and say so rather than re-add"

[worker.3]
targets = ["deps.md#8-parse-cache[fb0aa10250]"]
reading = "crates/shared/src/project.rs, crates/shared/tests/project.rs, design/deps.md \u00a78, state/spec-changelog/deps.md, state/decisions/conformance-005.md, design/core.md:1650-1690 (read-only)"
why = "one gap but a real one \u2014 the disk-file cache key contradicts conformance-005, so it needs either the cache or a CHANGE- record; sole holder of design/deps.md and spec-changelog/deps.md, and must not edit design/core.md, which worker 1 holds"
