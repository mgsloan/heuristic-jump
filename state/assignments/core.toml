# One round's assignments, written by `hj plan-merge` from the planner's
# answer. Regenerated every round; not history.
planned = "2026-08-04T04:18:22+00:00"

[worker.1]
targets = ["core.md#the-command-line[d2a209c7a8]", "core.md#two-modes-collect-and-replay[f2e74dce26]", "core.md#two-modes-collect-and-replay[eb424449b6]", "core.md#the-table-is-not-enough-a-replay-has-to-show-its-failures"]
reading = "crates/measure_core/src/{table,replay,truth,collect,cli}.rs, crates/measure_core/tests/, crates/measure_core/src/snapshots/"
why = "every target is measure_core's replay half \u2014 table determinism, truth.jsonl provenance, and the unjudged failure-listing section share table.rs and replay.rs, and two-modes can go clean"

[worker.2]
targets = ["rope-modifications.md#textsummary-is-converted-too[017e996a2d]", "rope-modifications.md#the-signatures[ba8ba1bb9a]", "core.md#vendoring-the-zed-crates[dcb3592e02]"]
reading = "vendor/rope/src/{rope,point,point_utf16,chunk,offset,offset_utf16}.rs, vendor/rope/tests/newtype_api.rs, vendor/rope/allowed-primitives.txt, vendor/README.md, design/rope-modifications.md"
why = "all three are inside vendor/ \u2014 TextSummary and the four signatures are the last two rope-modifications gaps, so that doc can go clean; must not edit crates/shared (worker 3 owns proto.rs)"

[worker.3]
targets = ["core.md#84-location-is-byte-based-and-this-fixes-a-real-inconsistency[f2b9c0b7e5]", "core.md#85-the-untagged-unions-are-the-actual-risk[cfd3a3fbdc]"]
reading = "crates/shared/src/proto.rs, crates/shared/tests/ (golden-traffic.jsonl, untagged/oracle tests), crates/driver/src/dispatch.rs"
why = "both are the wire types: WirePosition's missing line-only encode path and the corpus that exercises the untagged unions, both centred on proto.rs; the golden-corpus half may only be closeable as a decision if real pyright/gopls capture is unavailable"
