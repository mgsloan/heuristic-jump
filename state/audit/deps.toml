# Audit of design/deps.md. Written by harness/audit, never by hand.
# `design/loops.md` section 3. The section list is parsed out of the
# document, so it follows the document rather than drifting from it.

[section."deps.md#0-summary-table"]
state = "clean"
last_audited = "2026-08-04T23:43:10+00:00"

  [[section."deps.md#0-summary-table".minor]]
  claim = "the Where column places rayon, serde, tree-sitter and rustc-hash in driver, and insta in the workspace"
  note = "none is declared there \u2014 rayon exists only for vendor/*, insta is absent from [workspace.dependencies]. Consistent with 'each dep arrives with its first user', but the derived check only bans crates the table omits, so the presence direction is unverified"
  where = "crates/driver/tests/seam.rs:1738"

[section."deps.md#1-async-runtime-none"]
state = "clean"
last_audited = "2026-08-04T23:43:10+00:00"

[section."deps.md#2-channels"]
state = "clean"
last_audited = "2026-08-04T23:43:10+00:00"

[section."deps.md#3-lsp-types-our-own-not-lsp-types"]
state = "clean"
last_audited = "2026-08-05T00:43:32+00:00"

[section."deps.md#4-json"]
state = "clean"
last_audited = "2026-08-05T00:43:32+00:00"

[section."deps.md#5-text-vendored-zed-rope"]
state = "clean"
last_audited = "2026-08-04T23:43:10+00:00"

[section."deps.md#licensing-our-crates-are-mit-the-binary-is-gpl"]
state = "clean"
last_audited = "2026-08-05T01:40:56+00:00"

[section."deps.md#6-tree-sitter"]
state = "clean"
last_audited = "2026-08-04T23:43:10+00:00"

  [[section."deps.md#6-tree-sitter".minor]]
  claim = "`driver` depends on `tree-sitter` but on no grammar crate"
  note = "driver's [dependencies] names neither; tree-sitter reaches it through shared's re-export, and tree-sitter-rust \u2014 a grammar crate \u2014 is a dev-dependency, which the seam test carves out on purpose"
  where = "crates/driver/Cargo.toml:56"

[section."deps.md#7-file-enumeration-and-watching"]
state = "clean"
last_audited = "2026-08-05T01:40:56+00:00"

[section."deps.md#8-parse-cache"]
state = "clean"
last_audited = "2026-08-05T01:40:56+00:00"

  [[section."deps.md#8-parse-cache".minor]]
  claim = "`lru` bounds only entries, so driver wraps it with a byte ceiling"
  note = "the clippy.toml ban that protects the wrapper cites lru 0.18.1, while the workspace pins 0.16 and the lockfile resolves 0.16.4"
  where = "clippy.toml:64"

[section."deps.md#fxhashmap-and-fxhashset-are-the-default"]
state = "clean"
last_audited = "2026-08-04T21:12:07+00:00"

[section."deps.md#9-logging-and-tracing"]
state = "gaps"
last_audited = "2026-08-05T01:40:56+00:00"

  [[section."deps.md#9-logging-and-tracing".gap]]
  claim = "every line we emit gets a distinguishing prefix, and measure_core may default to `info` because a measure run has neither a child nor an editor"
  found = "measure_core installs a subscriber on a bare stderr with no LOG_PREFIX, and `collect` spawns a child language server with stderr inherited into that same stream \u2014 the interleaving the prefix rule exists for. Mechanisable: seam.rs already lists the crates that may install a subscriber; require each to name driver::PrefixedWriter"
  where = "crates/measure_core/src/measure_core.rs:184"
  found_at = "975523384418d70ab58801dd65e363bfcd28000d"

  [[section."deps.md#9-logging-and-tracing".minor]]
  claim = "the subscriber is installed by the crate that owns a process, and there are two processes"
  note = "the workspace manifest's comment on tracing-subscriber says it is installed in `heuristic_jump` and nowhere else"
  where = "Cargo.toml:108"

[section."deps.md#10-errors-one-enumerated-type-no-anyhow"]
state = "clean"
last_audited = "2026-08-05T00:43:32+00:00"

[section."deps.md#11-cli-parsing-clap"]
state = "clean"
last_audited = "2026-08-04T23:43:10+00:00"

[section."deps.md#12-testing"]
state = "clean"
last_audited = "2026-08-05T00:43:32+00:00"

[section."deps.md#13-explicitly-not-depended-on"]
state = "clean"
last_audited = "2026-08-04T23:43:10+00:00"

  [[section."deps.md#13-explicitly-not-depended-on".minor]]
  claim = "gix / git2 rejected because `ignore` reads .gitignore directly and we never need to talk to git"
  note = "neither crate is declared, so the entry holds, but the corpus checkout verification spawns git rev-parse and git status --porcelain \u2014 the stated reason is no longer true"
  where = "crates/measure_core/src/corpus.rs:393"

[section."deps.md#14-workspace-cargotoml-shape"]
state = "clean"
last_audited = "2026-08-04T23:43:10+00:00"

  [[section."deps.md#14-workspace-cargotoml-shape".minor]]
  claim = "a cargo-deny config asserting GPL reaches the graph only through vendor/rope and crates/similarity"
  note = "no deny.toml exists; the property is asserted by parsing `cargo metadata --offline` in a test, marked DECISION-core-021 provisional because deny.toml is outside the loop's writable paths"
  where = "crates/driver/tests/seam.rs:762"

[section."deps.md#15-clippy-in-workspace-toml"]
state = "clean"
last_audited = "2026-08-05T01:40:56+00:00"
