# Zed's, unchanged, so `vendor/` and `crates/` format the same way and a
# re-sync diff stays about the patches rather than about whitespace.
# https://github.com/rust-lang/rustfmt?tab=readme-ov-file#rusts-editions
edition = "2024"
style_edition = "2024"
