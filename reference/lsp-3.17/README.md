# LSP 3.17, vendored

Two files, both generated by `harness/reference-lsp`. **Do not edit them —
regenerate.** Denied to every loop, like everything else that is a source of
truth rather than a thing being built.

| | |
|---|---|
| `specification.md` | the whole 3.17 specification, Jekyll includes resolved. ~400KB, the authority |
| `shim-relevant.md` | the same text with everything the shim does not touch removed. ~110KB |
| `metaModel.json` | every request, notification and structure, machine-readable. 67 requests, 26 notifications, 324 structures |

Upstream is `microsoft/language-server-protocol`, branch `gh-pages`, at
`b7f5132c95261c0898ae5124e7a91707abc48fcd`. The published page is Jekyll: `specification.md` is a shell of 92
includes, so it is resolved at download rather than being useful as fetched.

## Why a trimmed copy exists

The shim is a proxy. It forwards every message it does not answer, so it needs
the base protocol exactly — framing, request and response shapes, error codes,
cancellation, ordering — and document synchronisation exactly, because it holds
authoritative text for every open document. It needs the definition family in
full because that is the one thing it answers itself.

Everything else is forwarded byte-identically without inspection
(`shim.md` §3), so those payload shapes are not the shim's business. Reading
them costs four hundred thousand tokens to learn nothing that changes the code.

The trim is **the specification's own text with sections removed**, never a
paraphrase. A summary of a protocol is a second source of truth, and the whole
point of vendoring this is to have one.
